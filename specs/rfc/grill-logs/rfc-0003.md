# RFC-0003 (Envelope Format) — grill log

Started 2026-07-14. Docket: wf_03b9d28b-20c (26 decisions, 7 conflicts, stress-verified).
Fixed context: RFC-0001/0002/0004 RATIFIED (single-principal, ADR-0001). RFC-0004 owns field-id registry MEMBERSHIP (RFC-0003 CARRIES the id<->name table); RFC-0002 signed-wins source authority; two-plane (keyed in signed_by, self-asserted in originator only). Hard cut at flag-day R.

## Layer 1 — inventory + field-id registry (D1-D6) — RATIFIED (Andreas 2026-07-14, bundle)
- **D1** RFC-0003 CARRIES RFC-0004's field-id table (replace §2 positional column with field-id column; per-field badges; demote §4 to "carries; RFC-0004 §4.1/§4.2 governs"). FOLLOW-UP: RFC-0004 vectors only cover ids 1-7 — ids 8-14 have NO oracle; file an RFC-0004 vector-matrix follow-up (one canonicalizeForSigning vector with all 14 signable fields; D31/D28 class). Land D1 first.
- **D2** closed contract PERMANENTLY (additionalProperties:false; unknown keys always reject; warn-on-newer spec_version does NOT license unknown keys). Correct the draft's distinguishing rationale sentence to match RFC-0004 §4.1 (don't over-claim the projection mechanism).
- **D3/D4/D5** codify: the add-a-field procedure (RFC-0003 inventory side); spec_version warn-on-newer normative; mutable-set membership at flag-day R.
- **D6** signed_by = ARRAY-ONLY at flag-day R (retire the single-object shim; stress-validated — emitters already array-form). CODE FOLLOW-UP: fix getActorIdentity regardless.
## Layer 2 — value grammars (D7-D10) — RATIFIED (Andreas 2026-07-14)
- **D7 = VERSION-AGNOSTIC uuid** — accept any RFC 9562 8-4-4-4-12 hex (version nibble NOT checked); v4/v7 RECOMMENDED for emit; lowercase on emit; reject urn:uuid: prefix. (id = stable shape; version buys no security absent a dedup contract.)
- **D8 = STRICT RFC 3339** — calendar-valid finite instant REQUIRED; uppercase T/Z (ABNF %s"T"/%s"Z"); emit UTC Z ms-precision (toISOString); one rule for timestamp/deadline/at (coheres with ratified `at`).
- **D9** source.stack goes live at R (derivation consumes seg 2; signed-wins: resolve from signed envelope, warn-not-reject on subject mismatch, never fabricate).
- **D10** type imports RFC-0001 kebab-strict segment terminal; 2-5 segment count stays envelope-law (stricter than subject is safe); total-length bound co-filed with RFC-0002.
## Layer 3 — size bounds (D11-D13) — RATIFIED (Andreas 2026-07-14)
- **D11** normative whole-envelope byte bound = 1,048,576 (1 MiB) receive-reject (matches deployed default); the transport-alignment sentence lives in RFC-0007 (precedent: RFC-0002's 255-octet subject ceiling).
- **D12** canonicalization structural caps (depth/width/node bounds) pinned as MUSTs (compose with RFC-0004 chain-cap 16).
- **D13** mutable-channel (extensions/economics) receive-side UTF-8 byte caps enforced at trust-boundary ingress — the qualitatively-different case (the only fields an ADVERSARIAL INTERMEDIARY can grow on someone else's signed envelope); numbers derive from RFC-0007/0009 riders.
## Layer 4 — extensions (D14) — RATIFIED (Andreas 2026-07-14)
- **D14** extensions discipline: resolve the reply_to contradiction; key-name rules; extensions/economics are the only open islands (additionalProperties elsewhere closed per D2).
## Layer 5 — DID-valued fields (D15-D21) — RATIFIED (Andreas 2026-07-14)
- **D15** two-plane placement enforced at BOTH schema-pattern + verify (keyed in signed_by[]; self-asserted surface/system in originator only).
- **D16 = FULL class-explicit DID for `source`** (Andreas override of bare-triple: "one address form everywhere, like IP; OK now pre-go-live"). source becomes did:mf:agent.{p}.{s}.{assistant} (a 6th DID-valued field, aligns RFC-0001 §2.2); migrate at flag-day R. D9 signed-wins still extracts the stack from the source DID.
- **D17** source↔chain provenance binding (completes RFC-0002 signed-wins: source's stack MUST reconcile with the innermost signer).
- **D18** agent-originator prefix binding enforced at VERIFY-time; RFC-0003 publishes the anchor-projection table — CORRECTED per stress: it MUST NOT contradict ratified RFC-0004 D16 (a principal/hub innermost signer is REJECTED as an anchor; do not invent an "agent-class signer → segments 1-2" row that violates D16).
- **D19 = ATTRIBUTE HUMANS VIA SURFACE** (my rec, Andreas agreed) — adapter-resolved humans: originator = did:mf:surface.{platform} + the surface's OPAQUE STABLE user-id as surface-asserted metadata; NOT email (PII-on-wire + surface doesn't verify it + mutable); NO new human class in v1; person/actor class = future Updates:0001. Rewrite flat-DID vectors same edit.
- **D20** target_assistant = agent-class only. **D21** stamped_by = {hub, stack}; economics.wallet = any-class role (per RFC-0009).
## Layer 6 — vectors (D22-D26) — RATIFIED (Andreas 2026-07-14)
- **D22** add invalid.json (the oracle is accept-only today — reject vectors required). **D23** vector DID epoch → rewrite flat-DID valid.json vectors to class-explicit dot-form (incl. source full-DID per D16). **D24** author the two-plane placement pair-set (surface-in-originator ACCEPT + rejects incl. surface-DID-in-signed_by reject); fix the mis-cite (RFC-0004 has no §4.5 — the verifier rule is elsewhere). **D25** reject-vector completeness bar (whole field grammars). **D26** sub-block ownership + cross-RFC citation sweep.

---
## ✅ GRILL COMPLETE — 26/26 (Andreas 2026-07-14). RFC-0003 fully decided.
NEXT: author RFC-0003 (Opus) → 2× verify → PR #230 → ratify single-principal.
FOLLOW-UPS: (RFC-0004) file a vector-matrix gap — ids 8-14 have no oracle (one canonicalizeForSigning vector, all 14 signable fields) [D1]. (cortex code, at R) fix getActorIdentity for array-only signed_by [D6]. source→full-DID migration rides flag-day R [D16].
