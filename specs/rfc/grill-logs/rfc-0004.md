# RFC-0004 (Envelope Signing & Canonicalization) — grill log

Started 2026-07-13. Docket: workflow wf_b1bd5982-8fa (6 facets → synthesize → stress).
Fixed context: RFC-0001 ratifications (class-explicit DIDs; two-plane taxonomy — signed_by[] is
KEYED-plane only; agent-prefix binding composes with chain verify; byte-exact comparison; HARD CUT
at flag-day R — all signatures regenerate at R; no wire rotation/revocation in v1).

## Layer 0 — stance (RATIFIED, Andreas 2026-07-13)

- **L0a — Default bias = TIGHTEN AT THE CUT.** Where live implementation and best crypto practice
  diverge (malleable signature encodings, no domain separation, loose canonicalization corners),
  the spec defaults to the STRICT form, effective at flag-day R (hard cut regenerates every
  signature, so strictness is nearly free now, prohibitive later). Each tightening still surfaces
  individually in the grill; this sets the presumption. NEVER flip a live posture as part of this —
  spec-level rules only.
- **L0b — Conformance target = INDEPENDENT-IMPLEMENTATION GRADE.** A second implementation
  (Go/Python/Rust, no access to the TS source) MUST be able to verify byte-for-byte from spec +
  vectors alone. Consequences: the JCS profile is pinned to the byte (number serialization, string
  escapes, key ordering, absent-vs-null); every corner gets a conformance vector; "read the TS
  source" is never part of the contract.

## Layer 1 — Canonicalization profile — IN PROGRESS

- **RFC4-D1 (bytes-to-sign addressing) = FIELD-ID INDIRECTION (Option B)** — Andreas 2026-07-13, OVERRIDING the docket recommendation (docket leaned freeze-now to avoid stacking crypto risk on flag-day R). Rationale (Andreas): federation is nascent, NO users on the network yet → the migrate-every-verifier cost the docket feared is near-zero NOW; greenfield is the cheapest moment to adopt the right long-term scheme. Sign over STABLE FIELD IDs, not literal field names, so a field rename is never again cryptographically breaking. Groundwork exists (msgpack/tag, src/serialization/).
  - **CASCADE (for the author + downstream):** (1) revisits RFC4-D2's parse model (its dependsOn); (2) requires a PERMANENT field-ID registry artifact — assign an id to each of the 14 SIGNABLE_FIELDS + an allocation rule for future fields; (3) RFC-0003's field inventory carries the id assignments (cite, don't duplicate); (4) this IS the "new crypto surface" — it gets a careful design in the authoring pass, NOT casually bolted onto flag-day R; (5) interacts with RFC4-D3 (SIGNABLE_FIELDS ownership seam).
  - Remaining Layer-1 items to grill next window: D2 (parse-then-recanonicalize + I-JSON constraints; stress-passed), D3 (dual-owned SIGNABLE_FIELDS membership seam RFC-0003↔0004).

### Layer 1 tail — RATIFIED (bundle, Andreas 2026-07-13, as recommended)
- **D2** parse-then-re-canonicalize + I-JSON (reject duplicate keys where detectable; no reliance on shadowed content; number-fidelity vectors).
- **D3** RFC-0004 OWNS SIGNABLE_FIELDS membership (now the field-ID registry per D1) — ownership sits where consequences are analyzed; fix envelope.abnf "T"/"Z" case defect; consolidate stamp vectors.
- **D4** integrity-BY-DEFAULT — new fields signed unless explicitly carved out; keep the partition check as a conformance vector.
- **D5** canonicalizer MUST reject non-plain objects; decoupled from the addressing decision (orthogonal).
- **D6** verifier resource bounds as ONE package (the §9 verifier-DoS OD): chain-length cap **16**, MUST-fail-cleanly + MAY-reject beyond the floor (registry allows a successor RFC to harden to MUST-reject).

## Layer 2 — Encodings & malleability — RATIFIED (Andreas 2026-07-13)
- **D7** canonical signature encoding = exactly-88 base64 on emit+verify, folded into flag-day R; ship maxLength/pattern bound now as WARN→reject. (convergent, tighten-at-cut)
- **D8 (verification equation) = STRICTEST, FULLY PINNED** — PureEdDSA, **cofactorless**, reject small-order on BOTH A and R, reject non-canonical point/scalar encodings, S<L. The intersection where every conforming lib agrees (noble/libsodium/Go disagree otherwise — the interop trap D8 exists to kill). myelin adds checks to its noble verifyAsync sites at R; edge-case vectors pin each rule. (independent-impl-grade)
- **D9 (domain separation) = ADD DOMAIN-SEPARATION PREFIX NOW** — bytes-to-sign = CONTEXT_TAG || canonical; rides flag-day R free; a metafactory-envelope signature is structurally unusable in any other protocol (kills cross-protocol NKey-reuse class, §8.8). Overrides docket's accept-risk rec. (tighten-at-cut)
- **D10** public-key interchange = base64-raw as the normative crypto-core constant (one §8 sentence); the wire carries NO key; NKey base32 is a valid local form that MUST bridge to base64-raw at boundaries.

## Layer 3 — Chain semantics — RATIFIED (Andreas 2026-07-13)
- **D11+D12 = ANCHOR AUTHORITY ON ORIGIN s[0].** s[0]=origin (attribution + authorization, truncation-safe); s[n-1]=current hop, AUTHENTICATION ONLY, MUST NOT drive authorization. **CODE FOLLOW-UP (file an issue): the live F-5 sovereignty ingress gate (myelin `validateIngress`) authorizes on s[n-1] — a strip-a-trailing-stamp tampering vector; MUST move its decision to the s[0]/origin anchor; fix lands at flag-day R.** D12: truncation finding + normative consumer rule (trailing stamps strippable); accept-vs-mitigate residual noted for the H4-successor RFC.
- **D13** append = attestation, NOT endorsement (v1); normative "append is not endorsement" + v2 forward pointer.
- **D14** hub-stamp MECHANICS pinned (verify.ts:207-267); vouching-authority SCOPE (may hub X stamp for identity Y) stays OPEN, blocked on Phase D — until then a trusted hub is omnipotent within its trusting registry; hub-trust grants MUST be treated accordingly.
- **D15** trust-vs-bytes split (#480/TC-2d) elevated to normative: short-circuit the TRUST check for the receiving stack's own DID, NEVER the BYTES check. (close same direction as D14 re the hub-stamp structural skip — flagged conflict.)
- **D16 = FAIL CLOSED:** an envelope whose chain can't establish the agent-prefix↔stack binding is REJECTED (dead on wire post-R); extraction/ordering/precedence per RFC-0001's lowest-index (truncation-safe) anchor.

## Layer 4 — Freshness / replay / DoS — RATIFIED (Andreas 2026-07-13)
- **D17 = ADMISSION-ONLY freshness control.** ±300s checked ONCE at trust-boundary ingress (first cross-boundary receipt; JetStream = stream entry, not consumer read); re-verifying a stored/replayed envelope MUST NOT re-apply the window (makes archive-replay + freshness coexist). **CODE FOLLOW-UP: the verifier re-checks freshness on every call today (verify.ts:19,63,130) — MUST separate admit-time from re-verify; lands at R.**
- **D18** replay: SHOULD general / MUST for task-dispatch admission (duplicate = double-execution). RFC-0004 owns replay-vs-redelivery vocab + consumer obligation; RFC-0007 owns Nats-Msg-Id + duplicate_window. NO nonce in signed bytes in v1.
- **D19** cheap-reject ordering: "all MUST hold; cheap tier before expensive per stamp" (not a rigid total order); negative vectors carry exactly one defect each. Must close before vectors freeze.
- **D20** stamp `at` = sole normative freshness anchor; chain monotonicity at SHOULD (§7.5 predicate, promote to MUST only on a zero-violation fleet audit). Cross-refs D17 (anchor meaningful only w.r.t. admission).

## Layer 5 — Postures & verification policy — RATIFIED (Andreas 2026-07-13; stress-reconciled, wf_b1bd5982-8fa — decisions HELD, justifications refined)
- **D21** define abstract verifier conformance CLASSES (deployment postures map onto them); a class claim covers every envelope the impl admits; trust-empty bypass recorded as a FINDING. [stress: decision sound; a 3rd-pillar citation is wrong — editorial fix at authoring]
- **D22** monotone reject: a NON-EMPTY signed_by that fails SIGNATURE/CHAIN verification MUST reject at every class; classes differ only on whether UNSIGNED is admissible. [stress refinement: scope to signature/chain ONLY — MUST NOT sweep §7.4 freshness in (freshness is admission-only per D17)]
- **D23** emitter: boundary-scope MUST sign; silent fallback-to-unsigned on boundary traffic = fail-open FINDING; local-scope fallback blessing = deferred Andreas+JC. [HOLD]
- **D24 = ENFORCE FLOOR ON ALL FEDERATED** — cross-principal ALWAYS gets enforcing-class verify (reject-unsigned + reject-invalid + resolve-peer-key); posture ladder governs local.* only; registry reachability = correctness. [stress: this is a REAL behavior change / implementation gap — NOT merely codifying deployed behavior (docket claim false on 2 of 3 arms) → CODE FOLLOW-UP.]
- **D25** announced/presence key confers NOTHING (the #1006 answer); re-sign-on-ingest promotion = deferred Andreas+JC. [HOLD]
- **D26 = STAMP-BEFORE-ADMIT** — under enforce, bound stack re-signs the gateway's unsigned first hop BEFORE the chain gate, drop-on-sign-failure. [stress: contradiction VERIFIED (enforce returns on empty_chain dispatch-listener.ts:1532 before re-sign :1600) → CODE FOLLOW-UP: reorder re-sign ahead of empty-chain gate.]
- **D27** §11 rejection tokens = normative result-object vocabulary (never on the wire); extend with chain-empty, at-outside-freshness, chain-too-long, canonicalization-depth. [HOLD]

## Layer 6 — Vectors & cross-RFC seams — RATIFIED except D32 (Andreas 2026-07-13)
- **D28** committed vector GENERATOR + CI harness + public operation surface — schedule FIRST in the vectors workstream (the conformance section is currently a claim about an artifact no code has run). [HOLD]
- **D29** ratification vectors bind the POST-CUT DOT-FORM (required by the compose vectors; consistent with the hard cut); flat form = illustrative appendix only + a vector-set versioning rule in CONFORMANCE.md/BCP-0001. [convergent with hard cut]
- **D30** fixed-test-key policy: publishing designated TEST private keys is standard; the hub TEST identity MUST move off did:mf:hub.metafactory at regen (collision with the reserved real identity); reservation cited from RFC-0001/registry (seam discipline). [HOLD]
- **D31** full vector matrix at authoring (one per §11 token, the stamp-1 chain-commit rejection, shim-divergence). [stress refinement: the only canonicalizer MUST-fail is non-finite numbers — scope the "JCS negatives" class to that, don't overclaim.] [HOLD]
- **D32 conformance unit — PENDING (the last grill fork).**

## CODE FOLLOW-UPS to file (from the grill, all land at flag-day R):
1. F-5 sovereignty ingress gate authorizes on strippable s[n-1] → re-anchor to origin s[0] [D11].
2. Verifier re-checks freshness on every call → separate admit-time from re-verify [D17].
3. Federation verification floor is a real gap (permissive stacks don't fully fail-closed on federated) → enforce it [D24].
4. Gateway re-sign runs AFTER the empty-chain gate under enforce → reorder re-sign ahead of it [D26].
## Layer 2 — Encodings & malleability — PENDING (docket)
## Layer 3 — Chain semantics — PENDING (docket)
## Layer 4 — Freshness / replay / DoS — PENDING (docket)
## Layer 5 — Postures & verification policy — PENDING (docket)
## Layer 6 — Vectors & cross-RFC seams — PENDING (docket)
