# RFC-0010 Rate-limit & Refusal Taxonomy — grill decision log

**Status:** RATIFIED (single-principal, Andreas, 2026-07-15, ADR-0001). The draft was authored from a single Andreas-approved resolution plan (main-loop, checkpoint commits) — most decisions were PRE-MADE by ratified siblings and the admission.md contract's own design; no open forks remained.

**The plan (approved 2026-07-15):**
1. **Rate-limit half = promote admission.md** verbatim-in-substance: KV bucket/key grammar, token-bucket windows, two-phase CAS multi-tier (first-refusing-tier; refusal read-only; no partial consume), degrade-loudly for named principals + FAIL-CLOSED for the anonymous principal, permanent-before-transient ordering.
2. **Refusal taxonomy:** closed kind registry = 4 transport mirrors (`cant_do|wont_do|not_now|compliance_block`) + `policy_denied` (RFC-0007 D4's eviction); object `{kind, detail, retry_after_ms}`; `not_now` transient (retry_after_ms REQUIRED), `term` FORBIDDEN for admission refusals; `policy_denied` permanent/term as deployed.
3. **Seam-consistency rule** (chartered at RFC-0007 §3, owned here): a mirror-kind object MUST agree with the co-carried transport token; mismatch = malformed; disposition ALWAYS routes off the token.
4. **admission.md retitled** to the substrate rate-limit contract (closes RFC-0006 OD-1); RFC-0006 D15's carved charset/KV-collision rule lands as §3.3 reject-not-coerce + the `admissionKeyPrincipalSegment` vectors.
5. Ratified under the standing authorization (zero retained open decisions — every §5-style question was resolved by a prior grill or the source contract).

Provenance: boundary ratified at RFC-0007 §3 (grill D8, charter amended 6e67bb9); `policy_denied` disposition = RFC-0007 D4; snake_case plane = RFC-0007 D2; requester-principal identity = RFC-0004 chain / RFC-0005 §6.1; failure posture = admission.md §8 (designed, not re-litigated).
