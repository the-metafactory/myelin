# RFC-0002 (Subject Namespace) — grill log

Started 2026-07-13. Docket: wf_866de30e-77d (29 decisions, 10 conflicts, stress-verified).
Fixed context: RFC-0001 + RFC-0004 RATIFIED (single-principal, ADR-0001). Subject NOT signed (RFC-0004). Hard-cut atomic field+subject flip at flag-day R.

## Layer 1 — @-segment / OD-6 — RATIFIED (Andreas 2026-07-13; stress-corrected)
- **D1 = FULL-DID** — the federated Direct/Delegate @-segment encodes the WHOLE class-explicit agent DID (`@did-mf-agent--{p}--{s}--{assistant}`), repeating {p}.{s}. OVERRIDES the docket's projection lean; stress FLIPPED it: the @-segment is a BLOCKING recipient-security gate (`validateCanonicalTaskRecipient`, dispatch-listener.ts:2399-2439, byte-compares vs `encodeDidSegment(target_assistant)`=whole DID, hard-drops on mismatch), full-DID is ZERO-code-delta (deployed publisher already builds the whole DID), projection needs net-new machinery + is unsound on legacy stackless (reintro cortex#1812) + can't serve non-agent classes.
- **D2 = EXEMPT the @-segment from the 63-octet per-segment cap** — bind it to the 255-total / msi≤63 rule; correct §5's "63-octet segment cap" phrasing. Cost accepted: long federated Direct subjects (~208 octets worst case); NATS handles it, cap isn't runtime-enforced.
- **D3 (ratify)** — codify the agent-class + home-binding invariant (aligns with RFC-0001 anti-impersonation prefix binding); full-DID serves all classes so it's not blocking.
- **D4 = MOOT** under full-DID (no projection ⇒ no reconstruction rule / decodeAssistantProjection needed).
- **D5 (ratify)** — full-DID keeps the legacy-stackless intermediate coherent + avoids a fabricated-stack on an identity-bearing segment.

## Layer 2 — source authority / OD-7 (D6-D10) — RATIFIED (Andreas 2026-07-13, bundle)
- **D6/D7 (OD-7 answer) = SIGNED-WINS.** Unsigned subject stack segment NEVER authoritative; signed representation governs (source.stack for source-addressed, receiver-context for receiver-addressed). Per-domain addressing table as closed normative record (dispatch=receiver-addressed; presence/agent.>=source-addressed; verdict/bid=per deployed builders, default source-addressed). Co-filed RFC-0003.
- **D8 (correction) = cite #1723 (seal-time) + #1812 (subscribe-time) as DISTINCT** fabricated-default points; do NOT conflate as "same root cause" (stress-verified).
- **D9 = auto-sync** the 5 "pending JC" citations → Ratified single-principal (ADR-0001).
- **D10 (ratify)** public. unattributed on-wire; origin only via signed_by; §8.3 security note; 3-prefix set closed.
## Layer 3 — domains (D11-D16) — RATIFIED (Andreas 2026-07-13)
- **D11** domain slot = open-with-reserved-roots; roots carrying normative shapes / JetStream partitions (tasks, dispatch, code, brain, _metrics) fail-closed against app misuse; slot itself not closeable across M7.
- **D12 = review.verdict.\* CANONICAL** — reserve the `review` root; retarget myelin's verdictSubject `code.pr.*`→`review.verdict.*` (what the deployed reviewer emits + request_id binds against).
- **D13** brain reserved root + normative `brain.>` / `tasks.>` stream-disjointness invariant (a wire contract every dual-stream peer respects); stream provisioning stays M7-owned.
- **D14 = `dispatched` CANONICAL** — lifecycle = received→dispatched→started→completed→aborted/failed (CONTEXT.md + cortex); retarget myelin enum + RFC ABNF `assigned`→`dispatched`. RFC-0002 owns the shape; closed enum homed in a dispatch-observability RFC.
- **D15** capability-id projects into the tasks slot (RFC-0002 owns the projection); RFC-0008 capability-ids MUST be subject-safe (charset ∩ segment — constrain the id, don't invent a subject encoder; resolves the `_` incompatibility).
- **D16** tasks position-4 = closed tagged union (capability | @-address); bid-request registered (closes OD-5); dead-letter/bid-request fail-closed as capability tags; tasks-vs-dispatch coexistence DECIDED (not reopened).

## Layer 4 — legacy grammar/migration (D17-D19) — RATIFIED (Andreas 2026-07-13)
- **D17** terminal grammar MANDATES the stack segment; `[stack.]` optionality is transitional (its removal IS the retirement); wire self-decidable by construction; BCP-0001 owns the retirement window.
- **D18** reject = MUST-not-emit at EVERY derivation entry point (fix the primitive gap: subjectFor throws on absent-stack subjects.ts:673, but deriveSubject silently emits legacy :622); stack-bound receivers reject via NATS matching, wildcard/audit consumers can't positionally.
- **D19 = DROP the default-for-matching carve-out** (§8.2); `>` already bridges both forms; fabricating `default` = #1723 silent-deafness; record namespace.md:94 warning as a §11 finding.

## Layers 5-6 — reserved/wildcards + vectors (D20-D29) — RATIFIED except D21/D24 (Andreas 2026-07-13)
- **D20** leading `_` = universal structural reservation marker; reserved-subject first-class; `_`-space exempt from app lowercase/start rules.
- **D22** admit `_INBOX.` by reference, uppercase-exempt (NATS's own byte-for-byte string).
- **D23** reserved-prefix registry = CLOSED table amended by `Updates:` (ADR-0001 living-spec; small+auditable).
- **D25-D29** author the missing vectors: dot-form encode + decodeDidSegment round-trip (D25); reserved-space rejects + metrics lowercase fix (D26); §2 MUST-reject prefix vectors + length check (D27); subscription positive-half + offer-reachability, gated on D24 (D28); seam-hygiene single-owner id + drift pairs (D29).
- **D21 = FOLD `_nak.` under `_audit.sovereignty.*`** (no separate top-level prefix; minimize the reserved-infra wall; RFC-0005's grill designs nak detail within _audit).
- **D24 = RESTRICT wildcard reach** — the grammar MUST NOT express a cross-scope or reserved-space wildcard: classification position can't be `*`/`>`; `>` tails can't cross the scope boundary or descend into `_`-space. Security-first (can't-express, not can-express-but-denied). Bind reject vectors (D28).

---
## ✅ GRILL COMPLETE — 29/29 (Andreas 2026-07-13). RFC-0002 fully decided.
Headline: @-segment = FULL class-explicit DID (stress-corrected from the docket's projection lean; it's a blocking recipient-security gate + zero-code-delta), exempt from the 63-cap.
NEXT: author RFC-0002 (Opus, from this log) → 2× verify → commit to PR #230 → ratify single-principal.
CODE FOLLOW-UPS (mostly land at flag-day R): retarget myelin verdictSubject code.pr.*→review.verdict.* (D12); retarget myelin dispatch enum + ABNF assigned→dispatched (D14); bind the stack-required check onto deriveSubject/deriveNatsSubject (D18, subjects.ts:622); constrain RFC-0008 capability-id charset subject-safe (D15); exempt @-segment from 63-cap in grammar (D2); drop default-for-matching carve-out (D19). @-segment full-DID = ZERO code delta.
