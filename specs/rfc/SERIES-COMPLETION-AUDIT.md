# Wire-Protocol RFC Series — Completion Audit (PLAN §6)

**Date:** 2026-07-15 · **Scope:** all 11 ratified RFCs (0001–0010 + BCP-0001), the grammars, the vectors, cortex as the consuming implementation, the CONTEXT glossaries, and the shared-library design.  
**Method:** 6-dimension adversarial audit (44 agents) — seam integrity, security composition, vector cross-binding, gap-closure, layer-language, implementation-drift/library — each finding refutation-verified. The synthesis agent hit the session limit; this report is assembled from the verified findings + per-dimension net-assessments (recovered from the run journal). Compact machine copy: `scratchpad/audit-extract.json` (session-local).

> **Bottom line.** No cross-RFC *wire contradiction* or *security hole* survived refutation — the series composes soundly. What the audit surfaced is (1) a cluster of **stale-ownership seams** (the capability-tag grammar is attributed three different ways across 0002/0003/0007/0008 after 0008 claimed sole ownership last, on 07-15), and (2) the **whole series is ratified against machinery that does not exist yet** — the `generated:` manifests and CONFORMANCE MUST-gates reference a generator + CI jobs that were never built, and every drift site the design doc inventories is still live. The protocol is specified; the *reuse* is not. That gap is the real subject of the two questions Andreas raised.

---

## 1. Layer language — the authoritative mapping (answers "seven layers but only two repos?")

**[MAJOR] ecosystem-wide (no single artifact states it): myelin-rfc0001/docs/architecture.md §3, cortex/docs/architecture.md §2+§4, compass/ecosystem/CONTEXT-MAP.md:44, specs/README.md**  
No artifact anywhere states the per-layer language mapping in one place — the principal's question ('we say seven layers but only Myelin and Cortex are named') is unanswerable from any single doc. The reconciled AUTHORITATIVE mapping is: ONE model, the 'Myelin layer model' (CONTEXT-MAP.md:44 reconciled name), numbered M1..M7 in cortex/compass and L1..L7 in myelin (same seven charters). L1/M1 CONNECTIVITY — owned upstream (nats-server leaf-node/TLS topology + NSC operator auth); NO RFC (declared out of scope, myelin architecture.md §4 'L1 (out of scope)', cortex architecture.md §4.1); implemented by nats-server + ~/.config/nats config, provisioned by cortex's network-join control plane. L2/M2 TRANSPORT — owned by myelin; RFC-0007 (NAK set, delivery modes, backoff, dead-letter, request-reply, correlation_id); implemented myelin src/transport/ (NATS+InMemory), consumed via cortex src/bus/nats/ + vendored src/bus/myelin/; sovereignty is ENFORCED here (RFC-0005:352). L3/M3 ENVELOPE — myelin; RFC-0003 (envelope format) + RFC-0002 (subject namespace — the L3 charter includes 'the NATS subject namespace' per myelin architecture.md:119, and RFC-0002:10 self-tags 'M3 working group') + RFC-0009 (Informational, economics mutable field); implemented myelin src/envelope.ts + schemas/envelope.schema.json + src/subjects.ts, cortex envelope validator; sovereignty DECLARED here. L4/M4 IDENTITY — myelin; RFC-0001 (did:mf identifiers) + RFC-0004 (signing & canonicalization, signed_by chain); implemented myelin src/identity/ + src/agent-identity/, cortex src/bus/verify-signed-by-chain.ts; sovereignty ATTESTED here. L5/M5 DISCOVERY — myelin; RFC-0008 (capability discovery, incl. the M5/M7 bridge at RFC-0008:514); implemented myelin src/discovery/ (signed self-advertisements), cortex static agents[]/offering config as the M7 side. L6/M6 COMPOSITION — myelin; NO RFC (see separate finding); partially implemented myelin src/composition/ + src/bidding/, cortex implements patterns ad-hoc. L7/M7 SURFACES — owned per-APP, not by myelin and not by one repo: cortex (collaboration surface + Mission Control), pilot (review loop), signal (observability), future apps; no RFC specifies an app (correct), but RFCs pin M7-side duties (RFC-0002:633/685 stream provisioning M7-owned; RFC-0007:166 compliance_block = M7 attestation refusal; RFC-0006:113 any M7 surface may implement the registry client). CROSS-LAYER: RFC-0005 (sovereignty, declared-L3/attested-L4/enforced-L2), RFC-0006 (membership/admission — an HTTPS control plane BESIDE the stack that gates L1 leaf credentials + the roster), RFC-0010 (refusal object, 0007⇄0010⇄0006 carve), BCP-0001 (meta/change control). The one-sentence clarification: myelin = the PROTOCOL contracts + reference library for layers 2–6 (not a running process); cortex = ONE L7 surface application among several; L1 = NATS infrastructure; a 'stack' is a cortex DEPLOYMENT unit, never a layer; the 11-RFC series specifies the L2–L5 contracts plus cross-layer concerns, and does NOT cover L1, L6, or L7. This paragraph (or its equivalent) should be added verbatim to specs/README.md and CONTEXT-MAP.md.  
<sub>Synthesized from: myelin-rfc0001/docs/architecture.md §3 per-layer table (L2 'src/transport/', L3 'src/envelope.ts … specs/namespace.md', L4 'src/identity/', L5 'src/discovery/', L6 'src/composition/, src/bidding/ … spec #10 still open', L7 '(other repos…)', L1 'out of scope'); cortex/docs/architecture.md §2 stack diagram + §4.1–4.6 per-layer contracts; CONTEXT-MAP.md:44 ('the M1–M7 architecture is the Myelin layer model'); RFC front matters and the layer self-tags quoted per-finding below. Each divergence between these sources is a separate finding.</sub>

**[BLOCKER] myelin-rfc0001/specs/README.md:5 (the series' mandated grounding entry point) + docs/design-shared-wire-codec.md:8,178**  
The RFC index's own scope sentence — 'myelin is **M3** of the seven-layer stack — envelope and namespace. The protocol specified here is…' — contradicts the contents of the very series it indexes (which specifies M2 transport, L4 identity/signing, and L5 discovery) and contradicts every other scope authority, all of which say myelin owns M2–M6.  
<sub>specs/README.md:5: 'myelin is **M3** of the seven-layer stack — envelope and namespace. The protocol specified here is implemented by **cortex**, **pilot**, **signal**…'. Versus, inside the same series: RFC-0007:82 'The myelin transport (layer M2) provides an abstract bus…'; RFC-0008:92 'Layer 5 (Discovery) of the myelin stack…'; RFC-0001:444 'the sole conformance authority for the did:mf identifier layer' (L4 per myelin docs/identity.md 'L4 — signed_by chain'). Versus the scope authorities: myelin CONTEXT.md:3 'Myelin is the M2–M6 protocol stack'; CONTEXT-MAP.md:13 'The M2–M6 protocol stack'; cortex docs/architecture.md:60 'myelin … owns M2–M6 of the stack (the protocol)'. The grounding contract in the same README (§'Grounding contract') tells agents to resolve this index first, so the mis-scope propagates. docs/design-shared-wire-codec.md doubles down: line 8 '**Owner layer:** myelin (M3)' and line 178 'myelin is M3. `src/wire/**` imports only myelin wire primitives … never a symbol, type, or concept from M4–M7' — under the correct M2–M6 scope that rule would exile myelin's OWN L4 identifier plane (identifiers.abnf, RFC-0001) and L2 transport tokens (transport.abnf, RFC-0007) from the codec that is supposed to be generated from exactly those grammars.</sub>

**[MAJOR] myelin-rfc0001 (L1–L7 everywhere) vs cortex + compass (M1–M7 declared canonical) vs the RFC series (mixes both); CONTEXT-MAP.md:12,13,44**  
The two repos number the SAME canonical model with different letter prefixes, no document declares the L↔M equivalence, and the reconciling authority (CONTEXT-MAP.md) itself uses both prefixes three lines apart — this is the direct mechanical cause of the principal's confusion.  
<sub>cortex docs/architecture.md:48: 'The canonical layer model is **M1–M7 — the Myelin stack** (myelin#7, JC + Andreas converged 2026-05-07)' and its §1.2 note blames 'Presenting both with L1..L7 numbering was the source of confusion'. Yet myelin's own canonical doc — docs/architecture.md, the doc that closes myelin#7's acceptance criterion ('Seven-layer model documented in myelin') — uses L1–L7 throughout (§2 diagram L1..L7, §3 table), as do myelin README.md:17-21 ('identity.md L4…') and myelin CONTEXT.md:12 ('L1 Connectivity … L7 Surfaces'). CONTEXT-MAP.md mixes: line 12 'cortex … The L7 collaboration surface', line 14 'signal … The L7 observability bundle', but line 13 'myelin … The M2–M6 protocol stack' and line 44 'the M1–M7 architecture is the Myelin layer model'. The RFC series splits the same way: M-form in RFC-0002:10 ('M3 working group'), RFC-0003:38 ('M3 of the seven-layer stack'), RFC-0004:191 ('the M3 message object'), RFC-0007:82 ('layer M2'), BCP-0001:37; L-form in RFC-0005:352 ('declared at L3 … attested at L4 … enforced at L2') and RFC-0009:146 ('outside the L4 signature'); RFC-0008 uses BOTH in one document (line 92 'Layer 5', line 311 'the L4 IdentityRegistry', line 514 'the M5/M7 seam'). cortex CONTEXT.md also mixes internally: line 3 'the M7 collaboration surface' but line 34 'the unchanged L1–L7 data plane' and line 36 'the surface is L7; the routing happens at L1–L3'.</sub>

**[MAJOR] myelin CONTEXT.md:13 (avoid-list) vs cortex CLAUDE.md:3,5,18 + cortex docs/architecture.md:48,71 vs RFC-0003:38, RFC-0008:92, specs/README.md:5**  
The canonical NAME of the model is contradicted across the boundary: myelin's glossary bans exactly the phrase ('the Myelin stack', 'the seven-layer stack') that cortex's generated CLAUDE.md and architecture doc use as THE canonical name — and three series artifacts use the banned aliases too. CONTEXT-MAP.md:44 reconciles on 'Myelin layer model', which makes cortex's docs and the three RFC-series usages the deviant ones.  
<sub>myelin CONTEXT.md:11-13: '**Myelin layer model**: The seven-layer protocol stack… _Avoid_: the Myelin stack (a `stack` is a cortex deployment unit — see the cortex bounded context), the seven-layer stack'. Versus cortex docs/architecture.md:48 'M1–M7 — the Myelin stack' and :71 'The canonical layered architecture is **M1–M7**, the Myelin stack'; cortex CLAUDE.md:3/5 'Layer-7 collaboration surface for the metafactory Myelin stack' and :18 'the **M7 application** of the metafactory Myelin stack'. Within the series: RFC-0008:92 'the myelin stack' (banned alias 1); RFC-0003:38 'the seven-layer stack' and specs/README.md:5 'the seven-layer stack' (banned alias 2). Per cortex CLAUDE.md, 'When a term crosses a repo boundary, the CONTEXT-MAP.md is authoritative' — CONTEXT-MAP.md:44 fixes 'Myelin layer model', so the fix is mechanical: replace the two banned aliases at the cited sites.</sub>

**[MAJOR] cortex/CONTEXT.md:28 (runtime entry), also :202, :253**  
cortex's glossary assigns wrong layer numbers under BOTH numbering schemes — it swaps Envelope and Identity (calls the signing identity 'M3' and the subject namespace 'M4') and calls policy 'M6' — contradicting cortex's own CLAUDE.md/architecture.md layer diagram and myelin's model.  
<sub>cortex CONTEXT.md:28: 'each keeping its OWN signing identity (M3), subject sub-namespace (M4), policy (M6) and NatsLinks (… M1)'. Both models put signing/identity at layer 4 and the subject namespace at layer 3: cortex CLAUDE.md §Architecture 'M3 ENVELOPE (myelin — schema + namespace) / M4 IDENTITY'; cortex docs/architecture.md:85-88 'M4 IDENTITY — verifiable principal per envelope / M3 ENVELOPE — message format · sovereignty metadata'; myelin docs/architecture.md:119 (L3 charter includes 'the NATS subject namespace') and :149 (L4 'verifiable principal for every envelope'). The same swap recurs at cortex CONTEXT.md:202 'The stack stays the sole cryptographic signer (M3)' and :253 'the principal is the human trust/policy root that scopes subjects and policy (M3–M6)'. 'Policy (M6)' additionally conflates Composition (pipeline/fan-out/request-reply per both architecture docs) with policy enforcement, which RFC-0005:352 places at L2 ('enforced at L2 (the F-5 engine wrapping the transport)').</sub>

**[MAJOR] cortex/CONTEXT.md:175 + cortex/docs/architecture.md:192 + CONTEXT-MAP.md:29 vs cortex/docs/architecture.md §4.6 + myelin docs/architecture.md L6**  
cortex uses 'M6' for two different things: its §4.6 layer contract defines M6 as myelin-owned Composition ('Spec not yet written'), while its glossary, its §7 text, and the CONTEXT-MAP row define the substrate HARNESS — a cortex runner component that executes one dispatch — as 'the M6 runtime layer'. Executing a dispatch on Claude Code is an M7 application concern under the model; the double meaning makes 'M6' unresolvable in cortex docs.  
<sub>cortex CONTEXT.md:175: 'The M6 runtime layer that executes a single **dispatch** on one execution substrate… Closed enum of HarnessId values: claude-code, bus-peer, openai-codex…'. cortex docs/architecture.md:192: 'the harness layer (M6) executes; the listener (M7 runner) orchestrates'. CONTEXT-MAP.md:29: 'adds `substrate harness` (M6 executor)'. Versus cortex docs/architecture.md §4.6 (M6 — Composition): 'Spec not yet written. Cortex implements *patterns* (pipeline, fan-out, request/reply) ad-hoc today; M6 will codify them' — i.e. M6 is a myelin protocol contract, not a cortex execution component; and myelin docs/architecture.md:73 L6 charter is 'Patterns for combining envelopes (pipeline, fan-out, request/reply, negotiation)', which does not include substrate execution.</sub>

**[MAJOR] the 11-RFC series as a set: specs/README.md index table (no composition RFC); myelin#10; myelin docs/architecture.md:73; cortex docs/architecture.md §4.6**  
L6/M6 Composition is the ONLY myelin-owned protocol layer with no RFC — the series covers L2 (0007), L3 (0002/0003/0009), L4 (0001/0004), L5 (0008) and cross-layer (0005/0006/0010, BCP) but leaves one of the five myelin layers contract-less, and nothing in specs/README.md declares this gap (its scope sentence claims the series is only M3 anyway — see the BLOCKER).  
<sub>specs/README.md:27-39 index — no composition title. grep across all 11 RFCs for composition/L6/M6 yields only passing mentions (RFC-0002: 1, RFC-0003: 2, RFC-0004: 1, RFC-0008: 1, rest 0); no RFC has composition in scope. myelin docs/architecture.md:73: 'L6 Composition … spec [#10] still open | partially implemented'. myelin README.md:170: 'L6 Composition — orchestrator + bidding in-tree; pattern spec still open (#10)'. cortex docs/architecture.md §4.6: 'Status | Spec not yet written.' If the set is to 'move as a coordinated whole' (PLAN §6), the coordinated whole should state that L6 is out of series scope and tracked by myelin#10 — or charter RFC-0011 for it.</sub>

**[MAJOR] myelin-rfc0001/docs/architecture.md:23,50,72 (the canonical seven-layer reference the RFCs and README point at)**  
The canonical layer doc's L7 row is stale and self-contradictory about what implements L7 today: it lists 'Grove' (retired — replaced by cortex) and 'CORTEX (capability AI)' (the superseded v4 layer name, which the SAME doc says this model supersedes) — so a reader cannot tell whether 'CORTEX' at L7 means the old v4 AI-layer or the cortex repo, and the repo that actually IS the main L7 surface today is never correctly named.  
<sub>myelin docs/architecture.md:23: 'L7 SURFACES — Grove · pilot · signal-collector · dashboards · CORTEX (capability AI)'; :50 (mermaid) 'L7 SURFACES<br/>Grove · pilot · dashboards · CORTEX'; :72 'L7 Surfaces | Applications consuming the stack | (other repos: grove, pilot, signal)'. Same doc :17: 'It supersedes the v4 nervous-system five-layer naming (MYELIN / AXON / DENDRITE / SYNAPSE / CORTEX) … CORTEX is repositioned as a Layer 7 application'. cortex CLAUDE.md:18-20: cortex 'replaces both `the-metafactory/grove` (legacy v0.29.0) and `the-metafactory/grove-v2` … as the canonical home'. myelin README.md:7 sends every reader here first ('Start here: docs/architecture.md'). Fix: L7 row should read 'cortex · pilot · signal · future apps'.</sub>

**[MINOR] myelin-rfc0001/CONTEXT.md:3 vs :11-13**  
myelin's glossary is internally inconsistent in its first sentence: line 3 describes myelin as 'the M2–M6 protocol stack' (M-numbering + the word 'stack') while the glossary's own layer-model entry eight lines later defines L-numbering and bans stack-phrasings.  
<sub>CONTEXT.md:3: 'Myelin is the M2–M6 protocol stack of the metafactory ecosystem'. CONTEXT.md:12-13: 'L1 Connectivity, L2 Transport, … L7 Surfaces … _Avoid_: the Myelin stack …, the seven-layer stack'. The scope claim (2–6) is correct; the notation contradicts the entry that governs it.</sub>

**[MINOR] myelin-rfc0001/specs/rfc/rfc-0005-sovereignty-and-boundary-crossing.md:85,128,352,548-564**  
RFC-0005 overloads the reserved term 'layer' with two ad-hoc decompositions in one document — the '§7 Two-Layer Crossing Contract' (NSC layer vs engine layer) and the 'three-layer model' (L3/L4/L2) — while myelin CONTEXT.md reserves 'layer' for the seven charters; a reader hits 'two-layer', 'three-layer', and 'seven-layer' meanings of the same word inside one RFC.  
<sub>RFC-0005:548 '## 7. The Two-Layer Crossing Contract' with :555-560 'the NSC … layer makes a crossing **possible** …; the engine layer makes it **safe**' — neither is one of the seven charters. RFC-0005:352: 'The three-layer model (informative): sovereignty is **declared** at L3 …, **attested** at L4 …, **enforced** at L2'. myelin CONTEXT.md:15-16: '**Layer**: One of the seven charters in the Myelin layer model… _Avoid_: tier, level, ring'. Suggested wording: 'two-GATE crossing contract' / 'the declare-attest-enforce split', keeping 'layer' for L1–L7.</sub>

---

## 2. The shared-library plan (answers "reusable libraries so we don't reimplement the wire")

**[BLOCKER] myelin-rfc0001 specs/grammar/transport.abnf:29-31 vs specs/grammar/envelope.abnf:103,164; specs/vectors/transport/valid.json vector `correlation-id/uppercase-accepted`**  
The two ratified grammars for the same wire field (`correlation_id`) contradict each other, and RFC-0007's own vector contradicts RFC-0007's own grammar — so the design doc's foundational CI gate 'vectors ⊂ grammar' fails on day one, and validators generated from RFC-0003 vs RFC-0007 disagree on what a legal correlation_id is.  
<sub>transport.abnf:30-31: `uuid = 8hexlc "-" 4hexlc ...` with `hexlc = DIGIT / "a".."f"` (lowercase-only), while envelope.abnf:164: `uuid = 8hexdig-ci ...` with `hexdig-ci = DIGIT / %x41-46 / %x61-66` (case-insensitive) — both bind `correlation_id` (envelope.abnf:152 'uuid — id (required) and correlation_id (optional)'; transport.abnf:29 `correlation-id = uuid`). Vector `correlation-id/uppercase-accepted` (input `550E8400-E29B-...`, expect ok:true) passes envelope.abnf but FAILS a regex generated from transport.abnf. transport.abnf:27's comment ('Emit lowercase; accept case-insensitive') sides with the vector against its own rule. Per vectors/README.md rule 3 ('Where a vector and the grammar disagree, the grammar governs and the vector is a defect') the vector is formally the defect, but the documented intent says the grammar is. Either way the generate-from-ABNF pipeline (design-shared-wire-codec.md §3.3 gate 3) trips immediately, and neither the grammars nor the generator design have any emit-profile vs accept-profile representation to express the intended asymmetry.</sub>

**[MAJOR] myelin-rfc0001 specs/CONFORMANCE.md 'myelin MUST' 1-3; .github/workflows/ (confidentiality-gate.yml, integration.yml, lint.yml only); RFC front-matter `generated:` fields; absent tools/abnf-gen**  
The ratified series' entire conformance machinery is fiction today: no generator exists (tools/abnf-gen absent in both the worktree and myelin main), no CI job validates ABNF, regenerates artifacts, checks vector-grammar consistency, or enforces `why` — yet CONFORMANCE.md states these as unconditional MUSTs and every RFC carries a `generated:` manifest naming hand-written files as generated.  
<sub>`ls tools/` → No such file or directory (both /Users/andreas/Developer/myelin-rfc0001 and /Users/andreas/Developer/myelin). `.github/workflows/` contains only confidentiality-gate.yml, integration.yml, lint.yml — grep for 'abnf' across package.json + workflows returns nothing. The artifacts RFC-0002 declares generated (src/segment-validators.ts, src/patterns.ts, src/subject-vocabulary.ts, schemas/envelope.schema.json) all exist as hand-written source. identifiers.abnf:39-42 even hand-writes the future 'generated regex' `/^[a-z](?:[a-z0-9]|-(?=[a-z0-9])){0,62}$/` into a comment. CONFORMANCE.md's own words condemn the current state: 'Hand-maintaining any downstream artifact reintroduces exactly the drift this replaces.' The design doc's P0 gate ('bun run gen --check green') has not landed while all 11 RFCs proceeded to Ratified.</sub>

**[MAJOR] specs/CONFORMANCE.md 'myelin MUST' item 2 vs rfc-0001-identifiers.md front-matter `generated:` comment ('regenerated at the flag-day cutover (§9)') and schemas/envelope.schema.json**  
CONFORMANCE.md's unconditional regenerate-and-diff gate contradicts RFC-0001's flag-day deferral: the committed schema still carries the flat pre-ratification DID pattern, which the ratified dot-form grammar rejects, so the MUST-2 gate — if ever implemented as written — fails on every RFC-0001/0002 artifact, and no document states which rule wins during the pre-flag-day window.  
<sub>CONFORMANCE.md: 'myelin MUST: 2. Regenerate the artifacts listed in each RFC's `generated` field and fail if the committed output differs.' — no window carve-out. RFC-0001 front-matter: 'generated: # DERIVED from `grammar`; regenerated at the flag-day cutover (§9).' schemas/envelope.schema.json still has `"pattern": "^did:mf:[a-z](?:[a-z0-9._]|-(?!-))+$"` at all 6 DID sites — a pattern that accepts class-tag-less flat DIDs (e.g. `did:mf:andreas`) which identifiers.abnf's ratified `method-specific-id = principal-msi / stack-msi / agent-msi ...` (class tag mandatory at position 0) rejects. The committed 'generated' output provably differs from the grammar right now, by design, with no reconciliation rule.</sub>

**[MAJOR] specs/grammar/*.abnf (7 of 10 files); specs/grammar/README.md rule 2; design-shared-wire-codec.md §3.2**  
The grammar files are not standalone-parseable ABNF and no composition/import mechanism is specified anywhere — a clean-room implementer (or the planned generator) cannot mechanically resolve the grammar without inventing an unspecified cross-file merge convention.  
<sub>Programmatic scan of defined-vs-referenced rules: subject-namespace.abnf references `lower`, `stack-slug`, `did-subject-segment`, `inbox-prefix`, `inbox-id` without defining them (subject-namespace.abnf:9 says 'imported from RFC-0001', :281 'referenced here, NOT redefined (rule 5)'); transport.abnf references `principal-id`, `stack-slug`, `capability-tag`; envelope.abnf references `segment`, `did-prefix`, `agent-msi`; capability-discovery.abnf and economics.abnf reference `did`, `lower`; admission.abnf references `principal-id`; rate-limit.abnf references `lower`. RFC 5234 has no import construct, so grammar/README.md rule 2 ('CI MUST validate that each file parses as ABNF. A grammar that does not parse is not a grammar.') is unsatisfiable as stated for 7/10 files, and design-shared-wire-codec.md §3.2 ('Input: every specs/grammar/*.abnf. It validates each parses as ABNF [RFC5234] first') specifies no resolution order, no namespace, and no conflict rule for cross-file references.</sub>

**[MAJOR] specs/grammar/identifiers.abnf:38-42, envelope.abnf:216-218 & :231, plus 8 'side-condition' comment sites across envelope.abnf**  
Normative constraints — the 1-63 octet segment bound, the 2..64 capability-tag bound, the ≥88 signature length, and several semantic conditions — live only in ABNF comments, so 'regexes are generated from the ABNF' is false for exactly the constraints that made the deployed regexes what they are; the generator has no machine-readable side-condition channel to read.  
<sub>identifiers.abnf:38-41: 'LENGTH: 1-63 octets per segment. The structural rule below does not encode the octet bound … the bound is a separate normative constraint, enforced by the generated regex: /^[a-z](?:[a-z0-9]|-(?=[a-z0-9])){0,62}$/' — the bound and the target regex are both prose. envelope.abnf:216-218: 'LENGTH SIDE-CONDITION: 2..64 chars … a bound the run structure below does not itself express, carried as a side-condition.' envelope.abnf:231: 'schema minLength 88 (a LENGTH SIDE-CONDITION).' grep counts 8 'side-condition' comment sites in the grammars. A regex emitted from the ABNF alone is strictly laxer than the normative constraint; neither the RFCs nor design-shared-wire-codec.md §3 define how the generator acquires these bounds (front-matter? structured comment syntax? per-rule annotation file?).</sub>

**[MAJOR] specs/grammar/envelope.abnf:224 vs capability-discovery.abnf:49 vs subject-namespace.abnf:185 (`capability` rule)**  
`capability-tag` — a single terminal alphabet — is defined three times with structurally divergent productions inside the ratified set, violating grammar/README rule 5 ('An alphabet defined twice is an alphabet that will drift') in the very documents that state it; a generator emits three different regexes for one terminal, and envelope.abnf's version structurally admits single-character tags the other two forbid.  
<sub>envelope.abnf:224: `capability-tag = cap-head *( "-" cap-run )` with `cap-head = lower *( lower / DIGIT )` — input "a" matches structurally (the 2..64 floor exists only in the :216 comment). capability-discovery.abnf:49: `capability-tag = lower 1*tag-sym *( "-" 1*tag-sym ) / lower 1*( "-" 1*tag-sym )` — a 1-char tag is structurally impossible ('Single-char tags are FORBIDDEN', :44). subject-namespace.abnf: `capability = lower ( 1*alnum *( "-" alnum-run ) / 1*( "-" alnum-run ) )` — a third spelling. All three claim to transcribe the same CAPABILITY_TAG_RE (src/patterns.ts:21-22). `sovereignty-mode` is likewise defined in both sovereignty.abnf and capability-discovery.abnf (identical today); `uuid` (the BLOCKER above) shows where duplicated terminals end up.</sub>

**[MAJOR] myelin-rfc0001 package.json exports (no ./wire, ./vectors, ./schemas); specs/CONFORMANCE.md 'Each consumer MUST'; cortex package.json:55; cortex src/bus/myelin/vendor/**  
The CONFORMANCE consumer obligations are unrunnable as shipped in PR #230: vectors and schemas are not exported from the package, so no consumer can satisfy MUST-1 ('Pin a version … and run its vectors in CI'); meanwhile cortex pins myelin by raw git hash, vendors the schema and validator with no divergence gate (MUST-3), and has zero conformance tests — every consumer is born non-conformant to a Ratified regime the moment the PR merges.  
<sub>Worktree package.json exports: `.`, `./subjects`, `./envelope`, `./identity`, `./sovereignty`, `./transport`, `./transport/websocket`, `./discovery`, `./composition`, `./bidding`, `./edge` — no `./vectors`, `./schemas`, `./wire`, so CONFORMANCE.md's verbatim import `from "@the-metafactory/myelin/vectors/identifiers/valid.json"` cannot resolve. cortex package.json:55: `"@the-metafactory/myelin": "https://github.com/the-metafactory/myelin.git#a69ecd7d…"` — the same illegible raw-hash pin design-shared-wire-codec.md §4.2 flags for pilot exists in cortex. cortex src/bus/myelin/envelope-validator.ts:5 ('schema is copied verbatim from ~/Developer/myelin/schemas/envelope.schema.json') + :67 imports the vendored copy; grep for any vendor-drift check or myelin-vector conformance test across cortex src/ + scripts/ returns none. (The vendored copy happens to be byte-identical to the worktree schema today — the identity is luck, not a gate.)</sub>

**[MAJOR] cortex src/cli/cortex/commands/stack.ts:75, network.ts:3838, src/bus/review-consumer.ts:1454+:1474, src/common/types/capability.ts:172, src/cli/cortex/commands/offer.ts:131**  
Every drift site the design doc inventories is still live, and several now accept identifiers the ratified RFC-0001 kebab-strict grammar forbids — cortex will mint underscore-bearing stack slugs and uppercase segments that no conforming parser can round-trip, and nothing (no vector run, no CI gate) measures this distance.  
<sub>stack.ts:75 and network.ts:3838: `const SLUG_RE = /^[a-z][a-z0-9_-]*$/` — permits `_` and trailing `-`, both forbidden by identifiers.abnf `segment` ('NO "_" … NO trailing "-"'), so `cortex stack create my_stack` succeeds today and produces an unparseable-at-R identity. review-consumer.ts:1454 first-hyphen DID decoder (`body.indexOf("-")`) and :1474 `SEGMENT_RE = /^[A-Za-z0-9][\w.-]*$/` (uppercase + underscore) still live. CAPABILITY_ID_REGEX `/^[a-z][a-z0-9_-]*(\.[a-z][a-z0-9_-]*)*$/` still at capability.ts:172 and offer.ts:131 — capability-discovery.abnf:57-63 records it as `capability-id-compound`, 'a named conformance defect to tighten'. Nothing from the design doc's P0-P5 has landed (no src/wire in myelin, no src/common/wire in cortex main — WP-2 lives only on branch feat/wp2-wire-identity-codec) while ratification completed 0001-0010.</sub>

**[MAJOR] myelin-rfc0001 docs/design-shared-wire-codec.md §4.2, §8 (WP-4), §10.1; vs rfc-0001 front-matter (ratified 2026-07-13), specs/CONFORMANCE.md 'Changing the wire', rfc-0010**  
The design doc — the only plan of record for the reuse question — is stale against the series it must now implement: its keystone open decision is already resolved, its change-control premise contradicts ADR-0001's v1 posture, and RFC-0010's refusal-object grammar postdates it entirely, leaving that wire surface unassigned in the library plan.  
<sub>§10.1: 'The DID encoding (WP-4 / cortex#1880) is the keystone. Until it's decided, parseDid stays ambiguous … This is the one decision blocking P3-P4' — but RFC-0001 ratified the class-explicit dot-form on 2026-07-13, so P3's premise ('parseDid predicate edit, its ok:true branch unreachable today') is obsolete. §4.2: the contract clock 'may only move through RFC-BCP-0001 … two signatures … and a dual-accept window' citing CONFORMANCE.md 'Changing the wire' — that section now reads 'A dual-accept window is NOT required in v1' under single-principal ratification. Header 'Relates to: RFC-0001…0009 + RFC-BCP-0001' — RFC-0010 (refusal object {kind, detail, retry_after_ms}, rate-limit.abnf, its own vectors) is absent, so §5's in/out table never allocates the refusal-object codec. Inventory line-refs have also drifted (offer.ts:125→131, stack.ts:72→75, network.ts:2684→3838).</sub>

**[MAJOR] rfc-0004-envelope-signing.md front-matter `generated:` list, second entry**  
RFC-0004's machine-readable front matter — which agents are instructed to ground on — lists its own normative grammar file as a *generated* artifact, a category error under the CONFORMANCE precedence chain: a manifest-driven regenerate gate would attempt to regenerate the normative source from itself.  
<sub>rfc-0004 front-matter: `generated:\n  - schemas/envelope.schema.json#/$defs/signedByStamp\n  - specs/grammar/envelope-signing.abnf  # canonical signature (exactly-88), public-key (base64-raw), field-id registry, context-tag` — but the same front matter declares `grammar: specs/grammar/envelope-signing.abnf`, and CONFORMANCE.md's precedence diagram has ABNF at the top ('generates ▶ regexes · JSON Schema patterns · parsers (listed in generated)'). The grammar cannot appear on both sides of the arrow; per grammar/README rule 3 ('Downstream artifacts are generated, never authored') this entry marks the RFC's only normative syntax artifact as downstream of itself.</sub>

**[MINOR] rfc-0006, rfc-0008, rfc-0009, rfc-bcp-0001 front-matter `generated:` fields vs rfc-0007/rfc-0010**  
The `generated:` field is inconsistently typed across the series — four RFCs write `generated:\n  - []`, which YAML-parses as a list containing an empty list (`[[]]`), not an empty list — so any manifest-iterating CI or tooling hits a non-string element on 4 of 11 documents.  
<sub>rfc-0006: `generated:  # artifacts DERIVED from grammar; none regenerated into myelin yet\n  - []`; identical `- []` shape in rfc-0008, rfc-0009, and rfc-bcp-0001. rfc-0007 and rfc-0010 use the correct scalar form `generated: []`. Trivial to fix, but these front-matter blocks are explicitly labelled 'Machine-readable front matter. Agents ground on THIS, not on prose.'</sub>

**[MINOR] specs/vectors/README.md 'Layout' vs actual directory contents (capability-discovery/vectors.json, subject-namespace/vectors.json, sovereignty/crossing.json, envelope-signing/{canonicalize,reject,sign-verify}.json, admission/valid.json, economics/valid.json)**  
Six of ten vector directories deviate from the README's declared valid/invalid/render layout — including 16 rejection vectors living inside a file named `valid.json` (admission) and an Informational-but-grammared RFC with zero rejection vectors (economics) — so CONFORMANCE.md's canonical import pattern only works verbatim for 4 RFCs and a mechanical vector-runner cannot assume the layout.  
<sub>Verified counts: admission/valid.json holds {ok:true: 22, ok:false: 16} — rejections in 'valid.json'; economics/valid.json is {ok:true: 10} with no invalid file despite rfc-0009 declaring `grammar: specs/grammar/economics.abnf` (vectors/README rule 4: 'Adversarial cases are mandatory'); capability-discovery and subject-namespace ship a single mixed `vectors.json`; sovereignty ships only `crossing.json`. All 271 vectors do carry the full field schema (id/rfc/kind/input/expect/why — zero misses), so this is layout drift, not content drift; but the vector-runner the library plan promises must either canonicalize the layout or grow per-RFC special cases.</sub>

---

## 3. Gap-closure vs the opening 209-gap audit (§6.5) + retained-open census (§6.6)

- **[BLOCKER]** RFC-0006's retained OD-4 keeps a live dual-decode transitional acceptance (sealed-secret v1-PSK + v2) open with its retirement explicitly unscheduled, directly contradicting BCP-0001 §7's ratified MUST that a transitional acceptance name its retirement release at the moment it opens — and the window is entirely absent from BCP-0001's own window inventory, which claims the legacy 5-segment subject form was the ONLY unnamed transition. <sub>(myelin-rfc0001/specs/rfc/rfc-0006-membership-and-admission.md:125-126 vs rfc-bcp-0001-wire-change-control-and-versioning.md §7 (~566-569, 587))</sub>
- **[MAJOR]** RFC-0006 OD-1 is stale and factually false: its live [OPEN DECISION] marker says the re-homing is 'blocked on drafting RFC-0010 … chartered but not yet drafted', but RFC-0010 is Ratified (2026-07-15) and itself declares it closes OD-1 — and the closure has no formal instrument, because RFC-0010's front matter declares `updates: []` against the frozen RFC-0006. <sub>(myelin-rfc0001/specs/rfc/rfc-0006-membership-and-admission.md:220 (restated :783-786) vs rfc-0010-rate-limit-and-refusal-taxonomy.md:96 + front matter :8)</sub>
- **[MAJOR]** RFC-0009 §5.5's blocked-on label points at a question RFC-0003 already resolved, and its 'economics is unbounded' claim contradicts RFC-0003 D13's ratified ingress byte-cap MUST; RFC-0009 never acknowledges the D13 co-ownership handoff that names §5.5 as owner of the numeric cap (zero references to D13/byte cap/ingress in 0009). <sub>(myelin-rfc0001/specs/rfc/rfc-0009-economics.md:356-361 (§5.5) vs rfc-0003-envelope.md:680-686, 713-716, 722)</sub>
- **[MAJOR]** The machine-readable retained-open inventory is inconsistent: the `openDecisions` front-matter field exists only in RFC-0003 (empty) and RFC-0004 (3 entries), while RFC-0006 (retained OD-4 + stale OD-1) and RFC-0009 (6 live §5 charter markers) carry their retained decisions in prose only — so an agent enumerating the series' open set from front matter (the layer every RFC says to ground on) counts 3 when there are actually 10 live retained ODs (+1 stale). <sub>(myelin-rfc0001/specs/rfc/ front matter: rfc-0003:27, rfc-0004:26 (present) vs rfc-0006, rfc-0009 (absent))</sub>
- **[MAJOR]** The per-gap trace demanded by PLAN §6.5 ('Every finding … 209 gaps … is closed by a ratified decision') is unauditable below dimension/critical granularity: the raw 209-gap inventory the opening audit says is 'preserved' (scratchpad/audit-raw.json + audit-digest.md) does not exist in myelin, the RFC worktree, or any committed path — and ~190 of those gaps never completed the refutation pass even at audit time. <sub>(myelin/docs/wire-protocol-gap-analysis.md:179 (§10) — referenced artifacts missing from both repos)</sub>
- **[MINOR]** BCP-0001's front-matter crossRefs comment cites 'RFC-0007 OD-4' as a still-open slice living in 0007, but 0007's grill (woven 2026-07-15, after BCP froze 2026-07-14) resolved it normatively as D19 — RFC-0007 now has zero live OPEN DECISION markers, so the pointer dangles. <sub>(myelin-rfc0001/specs/rfc/rfc-bcp-0001-wire-change-control-and-versioning.md:26 (crossRefs comment) vs rfc-0007-transport-and-reliability.md:320)</sub>
- **[MINOR]** Five of RFC-0009's deliberately-retained successor-charter markers carry 'blocked on: unfiled' — i.e. the blocking issue was never filed — leaving the Standards-Track successor's charter anchored nowhere outside the document, contrary to the series' close-out discipline that deltas become issues. <sub>(myelin-rfc0001/specs/rfc/rfc-0009-economics.md:326, 333, 342, 349, 376)</sub>

---

## 4. Security composition (§6.3) — cross-layer paths

- **[BLOCKER] rfc-0004-envelope-signing.md §5.5 D11 (line ~580) + §9 F-5 (line 987) vs rfc-0005-sovereignty-and-boundary-crossing.md §6.1 (line 484) + §12 (line 779)** — Two Ratified RFCs give directly contradictory normative rules for the SAME decision — sovereignty ingress authorization. RFC-0004 mandates anchoring on the origin s[0] and forbids driving authorization from the last hop; RFC-0005 makes keying on the last stamp a conformance MUST. An implementation cannot conform to both, and RFC-0005 never reconciles the conflict.
- **[MAJOR] rfc-0006-membership-and-admission.md §7.3/§7.4 (lines 514-587) vs rfc-0005-sovereignty-and-boundary-crossing.md §6 ingress decision (lines 478-543) + §10 (line 754)** — RFC-0006 makes network_id cryptographically load-bearing in admission (to close a cross-network confused-deputy), but RFC-0005's ingress authorization is network-blind: the §6 decision procedure keys only on (principal ∈ imported_principals, subject ∈ local_scope, requirements ⊆ max_capabilities) and never reads the network dimension. The network scoping RFC-0006 binds is silently dropped one layer up, permitting cross-network trust bleed that neither RFC flags.
- **[MAJOR] rfc-0005-sovereignty-and-boundary-crossing.md §2.4 (lines 266-283) vs rfc-0004-envelope-signing.md §5.5 D12 (line ~584)** — RFC-0005 enforces max_hop as a forwarding-TTL against len(signed_by chain), asserting the chain length as a trustworthy hop-count observable — but RFC-0004 D12 declares trailing stamps strippable, so an admitted adversary can tail-strip and re-append to undercount hops and evade the loop bound. RFC-0004 flags the analogous ingress case (F-5) but not the max_hop case, and RFC-0005 §2.4 acknowledges neither strippability nor D12.
- **[MINOR] rfc-0005-sovereignty-and-boundary-crossing.md §8 D7 (lines 606-641) composed with §2/§12 (lines 771-779) and RFC-0004 signing** — The D7-conformant signed sovereignty nak specifies subject, source, signed_by, and classification/pairing, but is silent on whether the nak envelope carries its own required 5-member sovereignty block. Since §2/§12 make every conformant envelope carry that block and the §8 recursion exemption is from validateEgress ONLY (not from schema validation), a schema-validating subscriber — the exact actor D7 point 2 fixes the source field for — would still reject a signed nak that omits the block, reintroducing the defect class D7 closed.

---

## 5. Confirmed deltas (post-refutation) — the fix list

*Every item below reproduced from `file:line` and survived an adversarial refutation pass. BLOCKER→MAJOR / MAJOR→MINOR marks a downgrade on verification.*

### D1. [BLOCKER] Security Composition (PLAN §6.3) — cross-RFC security-model composition across the 11-RFC wire-protocol series
**Where:** `rfc-0004-envelope-signing.md §5.5 D11 (line ~580) + §9 F-5 (line 987) vs rfc-0005-sovereignty-and-boundary-crossing.md §6.1 (line 484) + §12 (line 779)`  
**Finding:** Two Ratified RFCs give directly contradictory normative rules for the SAME decision — sovereignty ingress authorization. RFC-0004 mandates anchoring on the origin s[0] and forbids driving authorization from the last hop; RFC-0005 makes keying on the last stamp a conformance MUST. An implementation cannot conform to both, and RFC-0005 never reconciles the conflict.  
**Verify:** Reproduced from source, both sides quoted. RFC-0004 §5.5 D11 (rfc-0004-envelope-signing.md:574-584): authorization MUST anchor on verified origin s[0]; "A verifier/consumer MUST NOT drive an authorization decision from s[n-1]"; §9 F-5 (:987-989) names the live sovereignty ingress gate (validateIngress) as authorizing on the strippable last hop with "A strip-a-trailing-stamp tampering vector exists until it does." RFC-0005 §6.1 (:485-486) keys ingress on the last signed_by stamp via getLastStampPrincipal, and §12 (:779) makes it an unconditional conformance MUST ("key ingress on the last-stamp identity"), cemented by vector ingress/unknown-principal-permissive-ceiling-block (crossing.json) whose decision input is literally lastStampPrincipal — so an implementation that satisfies RFC-0004 F-5 (s[0] anchor) fails RFC-0005's vector suite; simultaneous conformance is impossible. All refutation lines fail: (1) "it's authentication not authorization" is closed by RFC-0005's own §10 (:678) "the engine checks *authorization*, not *authenticity*"; (2) RFC-0004's D11 parenthetical reconciles spec-vs-deployed-code only — RFC-0005's keystone D1 makes its §12 MUST the contract (spec leads deployment), yet presents last-stamp keying as the rule, never a named defect, with no flag-day-R sunset; (3) RFC-0005 contains zero references to s[0]/D11/origin-anchoring (grep verified; its own "F-5" at :40/:183 is the engine component name, a label collision, not RFC-0004's finding), RFC-0004 is absent from its crossRefs, §10's seven findings (:683-719) omit last-stamp strippability, and the grill log cites RFC-0004 only for chain-length and nak signing. Aggravator: RFC-0005 ratified 2026-07-15, two days AFTER RFC-0004 ratified D11/F-5 (2026-07-13), and its reference sweep added [RFC-0004] without carrying the reconciliation. BLOCKER stands: MUST vs MUST-NOT on the same decision plus a live, vector-cemented tampering vector invisible in the governing ingress RFC.

### D2. [BLOCKER] VECTOR CROSS-BINDING + GRAMMAR (PLAN §6.4)
**Where:** `myelin-rfc0001/specs/vectors/sovereignty/crossing.json:417-620 (vectors ingress/mapped-in-scope-allow, ingress/mapped-subject-outside-scope-block, ingress/mapped-capability-exceeds-ceiling-block, ingress/unknown-principal-permissive-allow, ingress/unknown-principal-reject)`  
**Finding:** The five RFC-0005 ingress fixtures — including the two ACCEPT vectors — are built on identities and signatures that every ratified sibling grammar rejects, and they contradict the D9 vector in the SAME file.  
**Verify:** Every evidence leg reproduces from source; no exemption exists. (1) IDENTITY: crossing.json:435/489/544 carry signed_by identity "did:mf:acme.reviewer" and :599/:653 "did:mf:stranger.agent". identifiers.abnf:153-167 defines the closed msi alternation (principal/stack/agent/hub/surface/system) and :113-114 states "Validation is FAIL-CLOSED: an unregistered tag is a REJECT (D8)". "acme" and "stranger" are not registered tags, so both DIDs are grammar-rejects; :148-150 forecloses the legacy-classless escape: "From flag-day release R (RFC §9 hard cut — no dual-accept, no staged windows) the legacy classless form is REJECTED AT DECODE." grep for pre-cut/historical/legacy/classless across specs/vectors/sovereignty/ and specs/CONFORMANCE.md returns nothing — no exemption marker. (2) SAME-FILE D9 CONTRADICTION: crossing.json:759-767 (ingress/agent-class-import-entry-rejected) mandates "imported_principals entries MUST be principal-class DIDs" and that "the matcher compares the principal component extracted from the last stamp's agent-class identity" — the accept vectors' imported_principals entry (:449/:503) is not principal-class (not even a valid DID) and their last stamp is not agent-class, so a D9-conformant matcher cannot produce the expected allow at :465-469 and :629-633. The file even proves the corrected forms were known: :698 uses "did:mf:principal.stranger" and :760 "did:mf:agent.acme.default.echo" in newer vectors, while the five contested vectors kept the pre-decision flat form. (3) SIGNATURE: "AA" (:436 etc.) fails envelope.abnf §9's deployed accept-grammar ("plus schema minLength 88 (a LENGTH SIDE-CONDITION)") and envelope-signing.abnf's ratified rule `signature = 85base64-char final-quantum-2bit "=="` (exactly 88 chars). (4) PRECEDENCE: specs/vectors/README.md:74-75 verbatim: "Vectors MUST be consistent with the ABNF. The ABNF is normative for syntax. Where a vector and the grammar disagree, the grammar governs and the vector is a defect" — and README:3-7 declares vectors "the normative conformance artifact" that "bind everyone". Considered downgrade angle (rule 3 self-resolves the conflict → vectors are mere defects): rejected, because rule 2 (README:72-73) requires wrong vectors be DELETED with an RFC changelog note, and the two accept vectors are the only positive conformance coverage of RFC-0005 §6.3 ingress mapping — deleting them leaves the ingress-allow path with zero accept coverage, and as shipped any implementation that jointly satisfies ratified RFC-0001/0004 decoding MUST fail these RFC-0005 vectors, i.e. the 11-RFC set is not jointly conformable. That is exactly the cross-RFC contradiction class the PLAN §6.4 vector-cross-binding gate exists to block. Also considered the "validateIngress is semantic, not wire-decode, so fixture fields are opaque" angle: rejected — rule 3 carries no operation-scoped carve-out, and the allow decision itself hinges on matching an identity the ratified grammar cannot decode. BLOCKER stands.

### D3. [BLOCKER] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `myelin-rfc0001 specs/grammar/transport.abnf:29-31 vs specs/grammar/envelope.abnf:103,164; specs/vectors/transport/valid.json vector `correlation-id/uppercase-accepted``  
**Finding:** The two ratified grammars for the same wire field (`correlation_id`) contradict each other, and RFC-0007's own vector contradicts RFC-0007's own grammar — so the design doc's foundational CI gate 'vectors ⊂ grammar' fails on day one, and validators generated from RFC-0003 vs RFC-0007 disagree on what a legal correlation_id is.  
**Verify:** CONFIRMED, reproduced from source at every cited pin. (1) transport.abnf:29-31 (RFC-0007, header: "This grammar is normative"): uuid = 8hexlc... with hexlc = DIGIT / "a".."f" — lowercase-only. (2) envelope.abnf:103,164 (RFC-0003): uuid = 8hexdig-ci... with hexdig-ci = DIGIT / %x41-46 / %x61-66 — case-insensitive, comment block explicitly binding the rule to "id (required) and correlation_id (optional)". Same wire field, two ratified grammars, disjoint accept sets on uppercase; RFC-0007 §8 even claims ownership ("correlation_id ... is defined here", rfc-0007 line 462) while RFC-0003's grammar also binds it — no import/precedence mechanism resolves the clash. (3) Vector specs/vectors/transport/valid.json:197-207 (correlation-id/uppercase-accepted, kind parseCorrelationId, input 550E8400-..., expect ok:true) fails a validator generated from transport.abnf's own rule. (4) The contradiction is internal to RFC-0007, not just vector-vs-grammar: §8.1 prose (line 458) says a receiver "MUST accept an upper- or mixed-case UUID" while the same section inlines the lowercase-only ABNF at lines 453-455. Applying vectors/README.md rule 3 ("the grammar governs and the vector is a defect") deletes the vector but leaves the grammar contradicting RFC-0007's own MUST — no self-consistent resolution exists in the ratified set. Refutation attempts fail: transport.abnf's rule is not a marked emit-profile (the accept-side parseCorrelationId vector exercises it, and its own comment "Emit lowercase; accept case-insensitive" encodes only the emit half); envelope.abnf's uuid is not a different context (its comment names correlation_id); and grammar/README rule 5 ("An alphabet defined twice is an alphabet that will drift") is itself violated — the drift it predicts is exactly what happened. Severity holds at BLOCKER under the audit's own definition: generated RFC-0003 vs RFC-0007 validators disagree on a legal correlation_id, and the design doc's vectors⊂grammar CI gate trips on day one.

### D4. [MAJOR (was BLOCKER)] SEAM INTEGRITY (PLAN §6.1)
**Where:** `myelin-rfc0001: specs/rfc/rfc-0003-envelope.md:484-486 + specs/grammar/envelope.abnf:212-226 vs specs/rfc/rfc-0008-capability-discovery.md:358-363 + specs/grammar/capability-discovery.abnf:49-51`  
**Finding:** The `capability-tag` rule is dual-owned in direct contradiction: RFC-0008 §4.1 declares itself "the single normative owner of the capability-identifier grammar — capability-tag, capability-id-compound, and the eventual converged capability-id" and forbids transcribing "a second copy (one owner per wire rule; REVISIONS C5)", while RFC-0003 §3.12 (Ratified 2026-07-14) states the alphabet "is co-owned with RFC-0002 §6.3" (repeated in its References at line 919: "co-owns the capability-tag (§6.3)") and transcribes a full second copy of the production in its normative, CI-validated grammar file — a copy that is structurally DIFFERENT from RFC-0008's.  
**Verify:** DOWNGRADE BLOCKER→MAJOR. Every cited line reproduces: rfc-0008-capability-discovery.md:358-362 claims "single normative owner of the capability-identifier grammar — capability-tag…", while rfc-0003-envelope.md:484-486 says the alphabet "is co-owned with RFC-0002 §6.3 … a cross-doc divergence RFC-0002 must reconcile" (stale — the reconciliation was decided in 0008 grill D1, rfc-0008:338/363), 0003's [RFC-0008] ref (line 931) credits 0008 only with §6.5 match-ordering, "0008" is absent from 0003's crossRefs, and the two ABNF productions structurally differ on single-char tags (envelope.abnf:224-226 admits "a"; capability-discovery.abnf:49-51 rejects it). But BLOCKER fails on three refutations. (1) No wire contradiction: all three RFCs state the identical normative rule for the identical deployed regex CAPABILITY_TAG_RE — RFC-0003 §3.12 "2–64 chars, starting with a letter, ending with a letter/digit, no leading/trailing/consecutive hyphens" ≡ RFC-0008 §4.1:368-371 ≡ RFC-0002 §6.3:582-583 — so conformant implementations cannot disagree on any string; "a" is rejected by 0003's own MUST ("2–64 chars") and envelope.abnf:216-218 explicitly carries the bound as a declared LENGTH SIDE-CONDITION. Vectors are consistent (capability-tag/single-char-forbidden rejects "a"; rfc-2 capability/reject-single-char rejects "x"; envelope requirements-bad-tag "a--b" rejects under both productions). (2) The "two different generated accept sets" scenario is hypothetical: the shared-wire-codec is an unbuilt design (rfc-0008 front-matter generated: is an empty "future:" list; 0003's lists only envelope.schema.json), and a bare-production generator mis-generates BOTH files identically since the max-64 bound is structural in neither — side-condition handling is a tool prerequisite under which both converge. (3) No MUST NOT is violated by 0003: 0008 §4.1's prohibition is addressed to RFC-0002 ("RFC-0002's subject grammar and capability taxonomy … MUST NOT transcribe a second copy"), REVISIONS.md C5 (lines 29-30) scopes the dual-claim fix to capability-id between 0002↔0008 only, and 0008 §4.1:366-367 itself cites 0003's enforcement copy without objection. Note the outlier is arguably 0008's expansive wording: ratified RFC-0002 §1.2:161-166 draws the boundary the same way 0003 does ("This document specifies the subject-position capability tag grammar (§6.3); the compound capability-id grammar … normatively owned by RFC-0008") — and 0002 §6.3:585-598 still transcribes a tag ABNF itself. What survives is a real three-way stale-ownership seam needing a sweep (fix 0003 §3.12 + ref:919 + crossRefs; align or annotate envelope.abnf's structural min-length; reconcile 0008 §4.1's tag-ownership wording with 0002 §1.2) — squarely the audit's own MAJOR class (dangling ref / stale status), not a cross-RFC wire contradiction or security hole.

### D5. [MAJOR] SEAM INTEGRITY (PLAN §6.1)
**Where:** `myelin-rfc0001: specs/rfc/rfc-0007-transport-and-reliability.md:306 + specs/grammar/transport.abnf:8-9,42 vs specs/grammar/subject-namespace.abnf:185`  
**Finding:** RFC-0007 imports the `capability-tag` terminal from the wrong owner: §5.2 states "`capability-tag` is an RFC-0002 terminal" and transport.abnf's import header says "capability-tag — RFC-0002 (Ratified)", but RFC-0002's grammar (subject-namespace.abnf) defines NO production named `capability-tag` — its rule is named `capability` (line 185). A clean-room implementer resolving transport.abnf's `dead-letter-subject = ... ".tasks." dead-letter-segment "." capability-tag` against the named source file finds no such rule.  
**Verify:** MAJOR stands. Reproduced from source: rfc-0007-transport-and-reliability.md:306 ("capability-tag is an RFC-0002 terminal") and transport.abnf:8/42 attribute the dead-letter-subject terminal to RFC-0002, but subject-namespace.abnf defines no `capability-tag` production — its rule is `capability` (line 185); `capability-tag` exists only in envelope.abnf:224 (RFC-0003) and capability-discovery.abnf:49 (RFC-0008), whose §4.1 declares itself "the single normative owner" of `capability-tag` and forbids second copies (REVISIONS C5). Grammar README rule 5 makes cross-file name resolution the normative mechanism, so the import dangles. One nuance softens the "wrong twice over" phrasing without refuting the finding: RFC-0002 D15 (subject-namespace.abnf:179) gives RFC-0002 ownership of the capability PROJECTION into the tasks subject slot, so attributing the dead-letter slot to RFC-0002 is semantically defensible — the fix may be renaming the import to RFC-0002's `capability` rather than re-attributing to RFC-0008. Not a BLOCKER: all three transcriptions encode the same CAPABILITY_TAG_RE, so no byte-level interop divergence. Secondary note (subject-prefix locally restating classification, transport.abnf:39 vs subject-namespace.abnf:71) also verified.

### D6. [MAJOR] SEAM INTEGRITY (PLAN §6.1)
**Where:** `myelin-rfc0001: specs/rfc/rfc-0010-rate-limit-and-refusal-taxonomy.md:22 (crossRefs) vs :355-368 (§9.1); also rfc-0005:19 and rfc-0003:20-27`  
**Finding:** Machine-readable front-matter crossRefs — the surface the series' own header says "Agents ground on THIS, not on prose" (rfc-0010 line 2) — under-list normative dependencies in three RFCs: RFC-0010's crossRefs are ["0006","0007"] while its own §9.1 Normative References list RFC-0001, RFC-0004, RFC-0005, and BCP-0001; RFC-0005's crossRefs are ["0001","0002","0003","0008"] while its 2026-07-15 change log records "References swept (0001/0003 Ratified; 0004/0006/0007 added)" and its body normatively builds on RFC-0004 (§3 attestation) and RFC-0007 (transport tokens); RFC-0003's crossRefs omit "0008" though §3.13 normatively defers sovereignty_required matching to "RFC-0008 §6.5" (twice) and the §8 handoff table cites it again.  
**Verify:** All three legs reproduce from source. (1) RFC-0010: crossRefs ["0006","0007"] (line 22) vs §9.1 Normative References listing RFC-0001/0004/0005/0006/0007/BCP-0001 (lines 359-368); normative body uses confirmed at line 201 (RFC-0004 chain; RFC-0005 §6.1) and line 150 (RFC-0005 NakReasonCode registry). (2) RFC-0005: crossRefs ["0001","0002","0003","0008"] (line 19) vs §13.1 listing RFC-0004/0006/0007 as normative (lines 814-816); its own 2026-07-15 change log (line 989) records "References swept (0001/0003 Ratified; 0004/0006/0007 added)" — References updated, crossRefs never reconciled. (3) RFC-0003: crossRefs omit "0008" (lines 20-26) though §3.13 (line 494) names RFC-0008 §6.5 "the single normative owner" of sovereignty_required matching and the §8 handoff table (line 730) cites it again; this leg is the weakest since RFC-0003 classes RFC-0008 as Informative (line 931), but the C10 sweep precedent (REVISIONS.md:46-47) added the identical owner-deferral crossRef to RFC-0002 and RFC-0005, and RFC-0003 also omits BCP-0001 from crossRefs despite listing it Normative (line 924). Refutation attempt via "crossRefs is deliberately narrower" fails: RFC-0007:20-27 and RFC-0002:24-31 include bcp-0001 and enumerate all referenced siblings, and C10 treats missing crossRefs as a defect fixed "with matching References entries." One wording nit: §3.13 cites RFC-0008 once, not twice. Severity MAJOR is correct — metadata/dangling-ref class, not a semantic wire contradiction.

### D7. [MAJOR] SEAM INTEGRITY (PLAN §6.1)
**Where:** `myelin-rfc0001: specs/rfc/rfc-0005-sovereignty-and-boundary-crossing.md:328-334 (and change log line ~990) vs rfc-0008-capability-discovery.md (no OD-5 anywhere)`  
**Finding:** RFC-0005 §2.6's recorded deferral names a target that does not exist as labelled: "owned by RFC-0008 OD-5 — the single normative owner ... decided at RFC-0008's grill" — but the authored/ratified RFC-0008 contains no "OD-5" designator anywhere (grep confirms its only OD- strings are back-references to RFC-0005's OD-7). The substance IS resolved — RFC-0008 §6.5 defines equality-match with ordering RESERVED (grill D3) and its change log line 1002 says "closes RFC-0005 OD-7" — but the pointer label dangles: a reader chasing "RFC-0008 OD-5" finds nothing under that name.  
**Verify:** Reproduced in full. rfc-0005:330 defers to "RFC-0008 OD-5" but RFC-0008 contains no OD-5 designator in ANY revision — git show 91656c8 (original draft) confirms the §6 docket was always §6.1..§6.5 with zero OD-N labels; the phantom label was coined in REVISIONS.md C1/C4 (2026-07-13 sweep) and propagated. RFC-0008's only OD- strings (565, 850, 1002) back-reference 0005's OD-7. Substance is bidirectionally closed (0008 §6.5 equality-matched v1/ordering RESERVED, grill D3; changelog 1002 "closes RFC-0005 OD-7") so not BLOCKER. Staleness confirmed: 0005:331 "next in the series queue", 0005:663 "closes when 0008 ratifies", and — worse than the finding stated — 0005:785 is a conformance sentence ("implementation MUST NOT attribute matching/ordering meaning until RFC-0008 OD-5 resolves") whose condition has already fired but whose anchor is unfindable, so an implementer could wrongly treat the match rule as still open. Aggravator: label collisions — 0008's grill "D5" is a DIFFERENT decision (§6.4; §6.5 is D3), and 0005 has its own OD-5 (permissive trust inversion). Two corrections: genealogy is "never existed" not "evaporated at authoring", and the fix is a ~8-site sweep (0005:324,330,653,663,785,918,989 + specs/grammar/sovereignty.abnf:80), not one line. MAJOR severity is exactly calibrated per the audit taxonomy (dangling ref + stale status in a Ratified doc).

### D8. [MAJOR] VECTOR CROSS-BINDING + GRAMMAR (PLAN §6.4)
**Where:** `myelin-rfc0001/specs/vectors/economics/valid.json:11-15,75-80 (economics/fully-populated-accepted, economics/unbounded-unknown-field-accepted, economics/wallet-reuses-did-mf-accepted)`  
**Finding:** Three RFC-0009 ACCEPT vectors pin the flat pre-cut wallet DID 'did:mf:ops-team', contradicting ratified RFC-0001 AND RFC-0009's own body, and the why asserts a blocker that is resolved.  
**Verify:** Reproduced in full. (1) economics/valid.json lines 14, 54, 78 pin flat 'did:mf:ops-team' in three ACCEPT vectors; vector 9's why (line 80) says 'Blocked on cortex#1880 (RFC §5.6)'. (2) RFC-0009 §2.4 (lines 223-230) and §5.6 header '[RESOLVED — 2026-07-12 — by RFC-0001 D12 (Ratified)]' plus line 372 ('former blocker... resolved') directly contradict that why — the why even cites §5.6, its own refuter. (3) Rejection is fail-closed and no dual-accept window exists: identifiers.abnf:148-150 ('hard cut — no dual-accept... legacy classless form is REJECTED AT DECODE'), and RFC-0009 Appendix A line 611 binds wallet-did = did (dot-form per lines 564-565); 'ops-team' has no '.', so position-0 is an unregistered class tag — same failure mode as inv/legacy-classless (identifiers/invalid.json:83-88). (4) Fixture split confirmed: envelope/valid.json:290 uses ratified 'did:mf:principal.andreas' for the same field. STRENGTHENING evidence beyond the finding: RFC-0009's own Appendix B (rfc-0009-economics.md:664-665) reproduces the SAME vector id economics/unbounded-unknown-field-accepted with input 'did:mf:principal.ops-team' while the shipped file has 'did:mf:ops-team' — same ID, divergent inputs, in one PR. Refutation angles tested: not a deliberate PRE-CUT pin (subject-namespace pre-cut vectors are explicitly labeled as such, e.g. vectors.json:8; these are not — they assert an unresolved blocker instead); the ratified sibling (RFC-0001 D12) creates the contradiction rather than resolving it. Severity MAJOR is correct (stale/unswept fixture + intra-RFC copy divergence from the 2026-07-13 cascade sweep, not a security hole — the normative body and grammar agree with each other). Fix: update the three vectors to class-explicit form (e.g. did:mf:principal.ops-team), rewrite vector 9's why to the resolved framing, and reconcile with Appendix B.

### D9. [MAJOR] VECTOR CROSS-BINDING + GRAMMAR (PLAN §6.4)
**Where:** `myelin-rfc0001/specs/vectors/envelope/valid.json:31,53,68 (all 7 signed_by stamps in valid.json, e.g. envelope/federated-signed-ed25519, envelope/hub-stamp-variant)`  
**Finding:** Every RFC-0003 ACCEPT-vector signature is 88×'A' with no '==' padding — lexically valid at RFC-0003's deliberately-loose layer but impossible under RFC-0004's ratified canonical form, so the 0003 fixtures do not 'sign per 0004' and flip to rejects at flag-day R.  
**Verify:** Fully reproduced. All 7 signed_by signatures in specs/vectors/envelope/valid.json are "A".repeat(88) with zero '=' padding (generate.ts:35 SIG88) — lexically valid under RFC-0003's deployed loose rule (envelope.abnf §9: 1*base64-char *"=", minLength 88) but impossible under RFC-0004's ratified canonical form (envelope-signing.abnf: signature = 85base64-char final-quantum-2bit "=="; 88 unpadded chars decode to 66 bytes, not 64). envelope.abnf §9 states the loose rule "TIGHTENS onto RFC-0004's signature at flag-day R", at which point all seven ACCEPT fixtures flip to rejects (rfc-0004-envelope-signing.md:656-658: non-canonical MUST be rejected, token signature-wrong-length; reject vector stamp/signature-wrong-length exists). Contrast: RFC-0004's own suite (envelope-signing/canonicalize.json, sign-verify.json) uses canonical 86+"==" signatures throughout, so the inconsistency is between sibling vector suites, not hypothetical. Refutation angles fail: the layer-scoping defense (generate.ts:32-34 pins SHAPE only) is undercut because every OTHER flag-day-R divergence in this suite is explicitly annotated (generate.ts:347, :395, :419) while these seven are not — and the generator comment mislabels SIG88 as "canonical-LENGTH". The proposed fix (86x'A'+"==", 'A' in final-quantum set {A,Q,g,w}) verifiably satisfies both layers. One nit: the vector why "signing bytes are RFC-0004's" is a deferral of the signature-content domain to 0004, not a conformance claim — the finding slightly overreads it, but the defect and MAJOR severity (unbound vector, per the audit's own taxonomy) stand.

### D10. [MAJOR] VECTOR CROSS-BINDING + GRAMMAR (PLAN §6.4)
**Where:** `myelin-rfc0001/specs/rfc/rfc-0006-membership-and-admission.md:1110-1173`  
**Finding:** RFC-0006's Appendix A fenced ABNF block is never closed: the closing ``` is glued to the end of the last grammar line, so the rendered code block swallows the entire '## Appendix B. Test Vectors' heading and its prose.  
**Verify:** Reproduced from source. rfc-0006-membership-and-admission.md:1110 ends with a glued ``` ("...\")```"), which is not a valid CommonMark closing fence (must be on its own line). The only subsequent line-start fences are ```json at :1159 (has an info string — invalid as a closer, becomes content) and the bare ``` at :1173. A real CommonMark parser (markdown-it) confirms: one fence info='abnf' spanning lines 1012-1173; the parsed heading list contains Appendix A and Appendix C but NOT 'Appendix B. Test Vectors' — the appendix, its vector-family enumeration (:1119-1156), and the representative JSON vector all render as literal text inside the ABNF code block. GitHub/GFM renders identically. Also verified the finding's qualifier: the ABNF at :1013-1110 (glued fence stripped) is byte-identical to specs/grammar/admission.abnf, so this is a structural/rendering defect, not grammar drift. Severity MAJOR stands (not BLOCKER: source text and the authoritative vector files under specs/vectors/admission/ survive; not MINOR: a whole appendix of a Ratified RFC disappears from every rendered view). Fix: move the closing fence after :1110 onto its own line.

### D11. [MAJOR] Gap closure vs opening audit (PLAN §6.5) + retained-open inventory (PLAN §6.6). Closure verdict: all 6 orphaned dimensions (signing, admission, transport, versioning, discovery-core, sovereignty-semantics) have Ratified RFC homes (0004/0006/0007/BCP-0001/0008/0005); ABNF + vectors columns of the coverage matrix are green (10 grammars, 10 non-empty vector dirs); criticals C-1..C-5 closed by ratified decisions. Live retained-open census: 0004=3 (properly inventoried, acceptable), 0006=2 (one stale, one conflicted), 0009=6 (deliberate charter, two defects), all others=0.
**Where:** `myelin-rfc0001/specs/rfc/rfc-0009-economics.md:356-361 (§5.5) vs rfc-0003-envelope.md:680-686, 713-716, 722`  
**Finding:** RFC-0009 §5.5's blocked-on label points at a question RFC-0003 already resolved, and its 'economics is unbounded' claim contradicts RFC-0003 D13's ratified ingress byte-cap MUST; RFC-0009 never acknowledges the D13 co-ownership handoff that names §5.5 as owner of the numeric cap (zero references to D13/byte cap/ingress in 0009).  
**Verify:** All cited lines reproduce. rfc-0009:356 says "blocked on: shared with RFC-0003 mutable-field bounds" and :361 points at "the general mutable-channel bounds question in RFC-0003" — but rfc-0003:27 has openDecisions: [], :714 says the grill "resolved every open decision of this document," and :722 hands the residual back ("economics cap → RFC-0009 §5.5"). D13 (rfc-0003:680-684, Ratified 2026-07-14) is a receiver MUST naming RFC-0009 §5.5 co-owner of the economics numeric cap; RFC-0009 (ratified 2026-07-15, one day AFTER the grill) contains zero occurrences of D13/byte cap/ingress (grep-verified), so the named owner is unaware of the handoff. Bonus seam the audit missed: 0009 §5.5 charters the successor to decide "maxProperties/maxLength bounds" while D13 ratified a different mechanism (per-channel UTF-8 byte cap at trust-boundary ingress) — implementing from 0009's charter picks the wrong knob. One leg overclaimed: "unbounded contradicts D13's MUST" is not a normative contradiction — 0009 §4 item 2 (line 305) scopes "Unbounded" to the schema (no maxProperties/maxLength), which stays literally true since D13's numeric value is unset; 0009 is Informational and imposes no counter-MUST; and 0003's own crossRefs still call the question "shared with this document's D13". Mitigants (insufficient to downgrade): 0009 §5 preamble retains OPEN DECISION labels "verbatim as the successor's charter," and the stale pointer does lead a reader to D13. Net: defect class is stale-status/unacknowledged handoff plus mechanism mismatch, squarely MAJOR under the audit's own rubric; soften "contradicts" to "fails to acknowledge".

### D12. [MAJOR] Gap closure vs opening audit (PLAN §6.5) + retained-open inventory (PLAN §6.6). Closure verdict: all 6 orphaned dimensions (signing, admission, transport, versioning, discovery-core, sovereignty-semantics) have Ratified RFC homes (0004/0006/0007/BCP-0001/0008/0005); ABNF + vectors columns of the coverage matrix are green (10 grammars, 10 non-empty vector dirs); criticals C-1..C-5 closed by ratified decisions. Live retained-open census: 0004=3 (properly inventoried, acceptable), 0006=2 (one stale, one conflicted), 0009=6 (deliberate charter, two defects), all others=0.
**Where:** `myelin/docs/wire-protocol-gap-analysis.md:179 (§10) — referenced artifacts missing from both repos`  
**Finding:** The per-gap trace demanded by PLAN §6.5 ('Every finding … 209 gaps … is closed by a ratified decision') is unauditable below dimension/critical granularity: the raw 209-gap inventory the opening audit says is 'preserved' (scratchpad/audit-raw.json + audit-digest.md) does not exist in myelin, the RFC worktree, or any committed path — and ~190 of those gaps never completed the refutation pass even at audit time.  
**Verify:** All three prongs reproduce from source. (1) PLAN.md:91 (§6 item 5) literally demands per-finding closure: "Every finding in wire-protocol-gap-analysis.md (6 orphaned dimensions, 209 gaps) is closed by a ratified decision." (2) gap-analysis.md:179 claims the raw inventory is "preserved at scratchpad/audit-raw.json" and the digest at "scratchpad/audit-digest.md" (line 116 defers the 94-medium+23-low tail to that digest "with per-item evidence") — but neither file exists in myelin, the RFC worktree, either repo's full git history (--all, --diff-filter=A), Spotlight, /private/tmp, or ~/.claude; the workflow resume script named at line 178 is also gone, so the replay path is dead. (3) gap-analysis.md:177 confirms "The remaining ~190 findings ... did NOT complete the refutation pass." Attempted refutations failed: no alternate per-gap traceability artifact (in-document inventory covers only ~30-40 critical/high slugs; no coverage-matrix file maps the 209 to RFC homes). Severity stays MAJOR, not BLOCKER: the finding correctly concedes dimension-level closure is verifiably green, C-1..C-5 close against quoted ratified text, and gap-analysis:180 states the structural finding doesn't depend on the unverified tail. A dangling reference to non-existent preserved artifacts that degrades the PLAN §6.5 gate from per-finding to dimension granularity is exactly the audit's MAJOR class. No grounds for DOWNGRADE: the document's preservation promise is false on this filesystem and §6.5 as written is unauditable.

### D13. [MAJOR] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `ecosystem-wide (no single artifact states it): myelin-rfc0001/docs/architecture.md §3, cortex/docs/architecture.md §2+§4, compass/ecosystem/CONTEXT-MAP.md:44, specs/README.md`  
**Finding:** No artifact anywhere states the per-layer language mapping in one place — the principal's question ('we say seven layers but only Myelin and Cortex are named') is unanswerable from any single doc. The reconciled AUTHORITATIVE mapping is: ONE model, the 'Myelin layer model' (CONTEXT-MAP.md:44 reconciled name), numbered M1..M7 in cortex/compass and L1..L7 in myelin (same seven charters). L1/M1 CONNECTIVITY — owned upstream (nats-server leaf-node/TLS topology + NSC operator auth); NO RFC (declared out of scope, myelin architecture.md §4 'L1 (out of scope)', cortex architecture.md §4.1); implemented by nats-server + ~/.config/nats config, provisioned by cortex's network-join control plane. L2/M2 TRANSPORT — owned by myelin; RFC-0007 (NAK set, delivery modes, backoff, dead-letter, request-reply, correlation_id); implemented myelin src/transport/ (NATS+InMemory), consumed via cortex src/bus/nats/ + vendored src/bus/myelin/; sovereignty is ENFORCED here (RFC-0005:352). L3/M3 ENVELOPE — myelin; RFC-0003 (envelope format) + RFC-0002 (subject namespace — the L3 charter includes 'the NATS subject namespace' per myelin architecture.md:119, and RFC-0002:10 self-tags 'M3 working group') + RFC-0009 (Informational, economics mutable field); implemented myelin src/envelope.ts + schemas/envelope.schema.json + src/subjects.ts, cortex envelope validator; sovereignty DECLARED here. L4/M4 IDENTITY — myelin; RFC-0001 (did:mf identifiers) + RFC-0004 (signing & canonicalization, signed_by chain); implemented myelin src/identity/ + src/agent-identity/, cortex src/bus/verify-signed-by-chain.ts; sovereignty ATTESTED here. L5/M5 DISCOVERY — myelin; RFC-0008 (capability discovery, incl. the M5/M7 bridge at RFC-0008:514); implemented myelin src/discovery/ (signed self-advertisements), cortex static agents[]/offering config as the M7 side. L6/M6 COMPOSITION — myelin; NO RFC (see separate finding); partially implemented myelin src/composition/ + src/bidding/, cortex implements patterns ad-hoc. L7/M7 SURFACES — owned per-APP, not by myelin and not by one repo: cortex (collaboration surface + Mission Control), pilot (review loop), signal (observability), future apps; no RFC specifies an app (correct), but RFCs pin M7-side duties (RFC-0002:633/685 stream provisioning M7-owned; RFC-0007:166 compliance_block = M7 attestation refusal; RFC-0006:113 any M7 surface may implement the registry client). CROSS-LAYER: RFC-0005 (sovereignty, declared-L3/attested-L4/enforced-L2), RFC-0006 (membership/admission — an HTTPS control plane BESIDE the stack that gates L1 leaf credentials + the roster), RFC-0010 (refusal object, 0007⇄0010⇄0006 carve), BCP-0001 (meta/change control). The one-sentence clarification: myelin = the PROTOCOL contracts + reference library for layers 2–6 (not a running process); cortex = ONE L7 surface application among several; L1 = NATS infrastructure; a 'stack' is a cortex DEPLOYMENT unit, never a layer; the 11-RFC series specifies the L2–L5 contracts plus cross-layer concerns, and does NOT cover L1, L6, or L7. This paragraph (or its equivalent) should be added verbatim to specs/README.md and CONTEXT-MAP.md.  
**Verify:** Refutation attempted and failed; the finding reproduces from source. (1) The strongest single-artifact candidate, myelin-rfc0001/docs/architecture.md §3 (lines 70–78), maps layers only to code+issues — grep confirms ZERO RFC-series references in the file (only RFC 8785/JCS at lines 154/220/265), so it cannot state which RFC binds which layer; its own L7 row (lines 23–24) lists retired "Grove" alongside "CORTEX (capability AI)", itself conflating cortex-the-app. (2) cortex/docs/architecture.md §2 (lines 69–99) has an M1–M7 table whose spec column is pre-RFC-series stale ("MY-400", "myelin spec TBD", envelope.schema.json); zero RFC-00 mentions file-wide. (3) CONTEXT-MAP.md:44 is a one-sentence stack-vs-layer reconciliation; myelin CONTEXT.md:11–13 names L1–L7 but maps no RFCs. (4) Sharpest reproducible contradiction: specs/README.md:5 states "myelin is **M3** of the seven-layer stack — envelope and namespace", contradicting cortex §2 (myelin owns M2–M6), myelin's own §3 table (code at L2–L6), and its OWN index 24 lines below (RFC-0007 Transport=L2, RFC-0001/0004 Identity=L4, RFC-0008 Discovery=L5). All supporting citations verified verbatim: RFC-0002 front matter "M3 working group"; RFC-0005 ~352 declared-L3/attested-L4/enforced-L2; RFC-0008 ~514 "M5/M7 seam"; RFC-0002:633/685 stream provisioning M7-owned; RFC-0007:166 compliance_block = M7 attestation refusal; RFC-0006 ~113 "non-cortex M7 surface"; architecture.md:119 L3 charter includes the NATS subject namespace. Severity MAJOR is correct under the audit's taxonomy (stale/false status statements at the series' entry point + no authoritative layer↔RFC↔repo mapping anywhere), not BLOCKER (no wire-level contradiction). Caveat on the proposed fix text: the finding's remediation paragraph is largely accurate but should note RFC-0002's subject grammar is shared by L2 addressing and L3 routing (architecture.md:127), and specs/README.md:5 needs correction (not just augmentation) since it currently asserts the false "myelin is M3" claim.

### D14. [MAJOR (was BLOCKER)] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `myelin-rfc0001/specs/README.md:5 (the series' mandated grounding entry point) + docs/design-shared-wire-codec.md:8,178`  
**Finding:** The RFC index's own scope sentence — 'myelin is **M3** of the seven-layer stack — envelope and namespace. The protocol specified here is…' — contradicts the contents of the very series it indexes (which specifies M2 transport, L4 identity/signing, and L5 discovery) and contradicts every other scope authority, all of which say myelin owns M2–M6.  
**Verify:** Facts CONFIRMED, severity refuted: BLOCKER → MAJOR. Every quote reproduces — specs/README.md:5 does say "myelin is **M3** … envelope and namespace," which contradicts myelin CONTEXT.md:3 / compass CONTEXT-MAP.md / cortex docs/architecture.md §1.3 (all "M2–M6") and the series' own contents (RFC-0007:82 transport=M2, RFC-0008:92 discovery=L5, RFC-0001 §11 identifier layer = L4 per docs/identity.md:1). But the BLOCKER bar (per the audit's own taxonomy) is a cross-RFC contradiction or security hole, and neither exists: (1) the eleven ratified RFCs are mutually CONSISTENT with M2–M6 — no ratified document contradicts another; the defect is one non-normative prose sentence in the index README, i.e. the stale-wording class = MAJOR. (2) The README's own Grounding contract (line 121ff: "normative only if it contains an RFC 2119 keyword in ALL CAPITALS… prefer the grammar to the prose") de-weights that sentence, so the propagation claim overstates blast radius. (3) Provenance: the collapsed "Original planned set" (README:52-60, RFCs 0001–0003 only) shows "envelope and namespace" is a stale leftover from the 3-RFC plan, not a designed mis-scope. (4) design-shared-wire-codec.md is "Status: Proposal — not a mandate" (line 7) and outside the ratified set; its line-178 "never M4–M7" rule is self-contradicted by its own §5 table (lines 165-168) which places RFC-0001 DID parsing and RFC-0008 capability canonicalization INSIDE the codec — a proposal-internal wording defect, not a series blocker. Fix is a one-line README correction (M3 → M2–M6) plus the design-doc scope line; no grammar, vector, or normative statement changes.

### D15. [MAJOR] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `myelin-rfc0001 (L1–L7 everywhere) vs cortex + compass (M1–M7 declared canonical) vs the RFC series (mixes both); CONTEXT-MAP.md:12,13,44`  
**Finding:** The two repos number the SAME canonical model with different letter prefixes, no document declares the L↔M equivalence, and the reconciling authority (CONTEXT-MAP.md) itself uses both prefixes three lines apart — this is the direct mechanical cause of the principal's confusion.  
**Verify:** All cited quotes reproduce exactly from source. M-form is declared canonical in cortex docs/architecture.md §1.2 ("The canonical layer model is M1–M7 — the Myelin stack (myelin#7...)" with the explicit note that L1..L7 numbering "was the source of confusion"), yet myelin's own docs/architecture.md — the doc closing myelin#7 — uses L-form exclusively (63 L-matches, zero M-matches), as do myelin README.md:14-17 and CONTEXT.md:12/70 (while myelin CONTEXT.md:3 says "M2–M6"). CONTEXT-MAP.md, the designated cross-context authority, mixes both: line 12 "The L7 collaboration surface" (cortex), line 13 "The M2–M6 protocol stack" (myelin), line 14 "L7 observability bundle" (signal), line 44 "the M1–M7 architecture is the Myelin layer model". All 10 RFC citations verified exact: M-form in RFC-0002:10, 0003:38, 0004:191, 0007:82, BCP-0001:37; L-form in RFC-0005:352, 0009:146; RFC-0008 mixes both prefixes in one document (92 "Layer 5", 311 "L4 IdentityRegistry", 514 "M5/M7 seam"). cortex CONTEXT.md also mixes (line 3 "M7" vs "unchanged L1–L7 data plane" / "surface is L7; routing happens at L1–L3"). A full-worktree sweep found NO document declaring L↔M equivalence — the only M1–M7 occurrence in myelin-rfc0001 is REVIEWING.md:34 ("myelin is M3"), a process doc that itself conflicts with CONTEXT-MAP's "myelin is M2–M6". Severity stays MAJOR, not BLOCKER: the layer numbers never disagree semantically (L3=M3=envelope everywhere), so nothing can be built wrong — but the reconciling authority itself is split with no binding mapping, a missing-declaration defect of the dangling-ref class.

### D16. [MAJOR] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `myelin CONTEXT.md:13 (avoid-list) vs cortex CLAUDE.md:3,5,18 + cortex docs/architecture.md:48,71 vs RFC-0003:38, RFC-0008:92, specs/README.md:5`  
**Finding:** The canonical NAME of the model is contradicted across the boundary: myelin's glossary bans exactly the phrase ('the Myelin stack', 'the seven-layer stack') that cortex's generated CLAUDE.md and architecture doc use as THE canonical name — and three series artifacts use the banned aliases too. CONTEXT-MAP.md:44 reconciles on 'Myelin layer model', which makes cortex's docs and the three RFC-series usages the deviant ones.  
**Verify:** All cited quotes reproduce verbatim. Ban: myelin-rfc0001/CONTEXT.md:13 avoids 'the Myelin stack' + 'the seven-layer stack'; adjudication: CONTEXT-MAP.md:44 fixes 'Myelin layer model' ('Never conflated'), authoritative per cortex CLAUDE.md:15. Deviants confirmed: cortex CLAUDE.md:3,5,18 (+:20), docs/architecture.md:48,71 (+:69,:564,:569 — more than cited); in-series: specs/README.md:5 + rfc-0003:38 ('the seven-layer stack' as referent name), rfc-0008:92 ('the myelin stack'). Refutation angles fail: (1) not descriptive-only — banned forms are used as the model's name; (2) not mere wording — 'stack' is normative wire vocabulary (RFC-0006 uses it 31x as protocol noun; did:mf:stack.{p}.{s} class at rfc-0006:394-395), so the alias collides with a wire-level term, exactly the avoid-entry's rationale; (3) no sibling resolves it — cortex's own CONTEXT.md:17,:348 side with myelin/the map, so cortex CLAUDE.md violates its own glossary-precedence rule (CLAUDE.md:14). MAJOR stands. One correction: remediation is not purely mechanical for cortex CLAUDE.md — it is generated (compass template + agents-md.yaml + docs/agents-md/architecture.md; :3/:5 come from the description placeholder), so the fix routes through the template sources, and arc upgrade compass has ~165-repo blast radius (edit section file + CLAUDE.md block directly per memory note). Bonus adjacent instance not in the finding: rfc-0006:195 'the metafactory stack'.

### D17. [MAJOR] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `cortex/CONTEXT.md:28 (runtime entry), also :202, :253`  
**Finding:** cortex's glossary assigns wrong layer numbers under BOTH numbering schemes — it swaps Envelope and Identity (calls the signing identity 'M3' and the subject namespace 'M4') and calls policy 'M6' — contradicting cortex's own CLAUDE.md/architecture.md layer diagram and myelin's model.  
**Verify:** CONFIRMED at MAJOR. The swap is real and reproduced from file:line on both sides. cortex CONTEXT.md:28 assigns "signing identity (M3), subject sub-namespace (M4), policy (M6)"; cortex's own CLAUDE.md:26-27 ("M4 IDENTITY / M3 ENVELOPE — schema + namespace"), cortex docs/architecture.md ("M4 IDENTITY — verifiable principal per envelope"; "M3 ENVELOPE — message format · sovereignty metadata"), and myelin docs/architecture.md (L3 charter includes "the NATS subject namespace"; L4 = Identity, hosting src/identity/sign.ts+verify.ts) all put signing at 4 and namespace at 3 — the swap holds under both numbering schemes. The swap recurs at CONTEXT.md:202 ("sole cryptographic signer (M3)"). "Policy (M6)" is also wrong: L6/M6 = COMPOSITION in both architecture docs, and ratified RFC-0005 (~line 351-353) places sovereignty enforcement at L2 (F-5 engine), declaration at L3, attestation at L4. CONTEXT.md even self-contradicts: line 328 correctly uses "M3 payload encryption" (envelope-layer, ADR-0019). Severity is aggravated because CLAUDE.md declares CONTEXT.md authoritative over instinct. One caveat: the :253 citation is the finding's weakest leg — "(M3–M6)" there is a coarse range, not a layer assignment, and subjects genuinely are M3; :253 is loose wording, not a recurrence of the swap. That trims one cited site but does not downgrade the finding: :28 and :202 carry it. MAJOR (doc-consistency defect, not a wire contradiction or security hole) is the right tier.

### D18. [MAJOR] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `cortex/CONTEXT.md:175 + cortex/docs/architecture.md:192 + CONTEXT-MAP.md:29 vs cortex/docs/architecture.md §4.6 + myelin docs/architecture.md L6`  
**Finding:** cortex uses 'M6' for two different things: its §4.6 layer contract defines M6 as myelin-owned Composition ('Spec not yet written'), while its glossary, its §7 text, and the CONTEXT-MAP row define the substrate HARNESS — a cortex runner component that executes one dispatch — as 'the M6 runtime layer'. Executing a dispatch on Claude Code is an M7 application concern under the model; the double meaning makes 'M6' unresolvable in cortex docs.  
**Verify:** Reproduced from every cited line. Side A: cortex/docs/architecture.md:79 diagram and §4.6 (lines 281-287) define M6 as myelin-owned COMPOSITION ("Spec not yet written… myelin#10… Cortex's dependency: None at v1"), matching myelin/docs/architecture.md:71 L6 charter ("Patterns for combining envelopes (pipeline, fan-out, request/reply)"), which contains no substrate execution. Side B: cortex/CONTEXT.md:175 ("Substrate harness… The M6 runtime layer that executes a single dispatch… Closed enum of HarnessId values: claude-code, bus-peer, openai-codex…"), CONTEXT.md:324 (same), cortex/docs/architecture.md:192 ("the harness layer (M6) executes; the listener (M7 runner) orchestrates"), and compass/ecosystem/CONTEXT-MAP.md:29 ("adds `substrate harness` (M6 executor)"). The contradiction is self-contained within each doc — architecture.md:192 vs its own §4.6; CONTEXT.md:175 vs its own line 327 ("Myelin (M2–M6)… stays dumb; application smarts… live at M7") — so no two-vintage refutation holds. No ratified sibling resolves it: the 11 RFCs contain zero normative occurrences of "M6"; no RFC defines the M-layer mapping. Severity MAJOR is correct: no wire-level token carries "M6" (HarnessId is a local enum), so not a BLOCKER, but it is an unresolvable layer-term collision across three canonical docs whose declared authority rules (CONTEXT.md wins terminology; architecture.md wins "what cortex IS") point in opposite directions — squarely the rubric's MAJOR class.

### D19. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `myelin-rfc0001 specs/CONFORMANCE.md 'myelin MUST' 1-3; .github/workflows/ (confidentiality-gate.yml, integration.yml, lint.yml only); RFC front-matter `generated:` fields; absent tools/abnf-gen`  
**Finding:** The ratified series' entire conformance machinery is fiction today: no generator exists (tools/abnf-gen absent in both the worktree and myelin main), no CI job validates ABNF, regenerates artifacts, checks vector-grammar consistency, or enforces `why` — yet CONFORMANCE.md states these as unconditional MUSTs and every RFC carries a `generated:` manifest naming hand-written files as generated.  
**Verify:** Reproduced from source; the finding survives at MAJOR, with two precision corrections that do not change the verdict.

CONFIRMED core: (1) `tools/` does not exist in either /Users/andreas/Developer/myelin-rfc0001 or /Users/andreas/Developer/myelin ("No such file or directory" both). (2) `.github/workflows/` contains exactly confidentiality-gate.yml, integration.yml, lint.yml; grep for 'abnf' across workflows + package.json returns zero hits; package.json scripts are only test/typecheck*/lint*/bench — no `gen`, no `gen --check`. (3) specs/CONFORMANCE.md:86-91 states unconditional present-tense obligations: "myelin MUST: 1. Validate every specs/grammar/*.abnf parses as ABNF [RFC5234]. 2. Regenerate the artifacts listed in each RFC's `generated` field and fail if the committed output differs. 3. Fail if any vector lacks a `why`." None of the three has any mechanical realization in the repo. (4) Worse than the finding states: CONFORMANCE.md:5-8 defines conformance as "passes every vector," yet no test in src/ or tests/ loads specs/vectors/** (only the two self-contained generate.ts scripts and one false-positive string in policy-store.test.ts reference anything vector-like) — so myelin, the reference implementation, does not conform to its own Ratified RFCs by its own iff-definition, and no gate measures that. (5) identifiers.abnf:38-43 confirmed: the "generated regex" /^[a-z](?:[a-z0-9]|-(?=[a-z0-9])){0,62}$/ is hand-written into a grammar comment. (6) The aspirational-reading defense fails: CONFORMANCE.md explicitly conditionalizes its change-control section on ADR-0001 ("dual-accept window is NOT required in v1") but leaves the "CI requirements" section unconditional — the authors knew how to phase obligations and did not phase these.

Precision corrections (wording, not severity): (a) CONFORMANCE.md never names `tools/abnf-gen` — that path appears only in docs/design-shared-wire-codec.md:39,87,138,213,248. The MUST-gates require *a* generator without naming one; the substance (MUST 2 is unsatisfiable today) holds. (b) "every RFC carries a `generated:` manifest naming hand-written files as generated" is overstated: RFC-0007:19 and RFC-0010:19 declare `generated: []`; RFC-0001:19 self-annotates "DERIVED from `grammar`; regenerated at the flag-day cutover (§9)" and RFC-0006:19 "none regenerated into myelin yet" — these manifests are declared regeneration *targets*, not false claims of current generation. RFC-0002:19-23 (Ratified 2026-07-14) however lists src/segment-validators.ts, src/patterns.ts, src/subject-vocabulary.ts, schemas/envelope.schema.json with no pending annotation, and all exist as hand-written source, while CONFORMANCE.md:57 asserts "Everything downstream is generated or checked, never authored" in the present tense — so the drift-window contradiction is real for RFC-0002 even under the deferred-cutover reading.

Severity stays MAJOR (per the audit's own taxonomy: unbound machinery/stale-state, not a cross-RFC wire contradiction or security hole). Key evidence paths: /Users/andreas/Developer/myelin-rfc0001/specs/CONFORMANCE.md:5-8,57,73-75,86-91; specs/rfc/rfc-0002-subject-namespace.md:19-23; specs/rfc/rfc-0001-identifiers.md:19-22; specs/rfc/rfc-0007-transport-and-reliability.md:19; specs/rfc/rfc-0010-rate-limit-and-refusal-taxonomy.md:19; specs/grammar/identifiers.abnf:38-43; .github/workflows/{lint,integration,confidentiality-gate}.yml; package.json:21-29; docs/design-shared-wire-codec.md:87,248.

### D20. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `specs/grammar/*.abnf (7 of 10 files); specs/grammar/README.md rule 2; design-shared-wire-codec.md §3.2`  
**Finding:** The grammar files are not standalone-parseable ABNF and no composition/import mechanism is specified anywhere — a clean-room implementer (or the planned generator) cannot mechanically resolve the grammar without inventing an unspecified cross-file merge convention.  
**Verify:** Core claim reproduces and is STRONGER than filed, but two evidence sub-claims must be corrected. REPRODUCED: 7/10 .abnf files reference rules defined only in sibling files (admission, capability-discovery, economics, envelope, rate-limit, subject-namespace, transport — verified programmatically); design-shared-wire-codec.md §3.2 specifies only "validates each parses as ABNF first" with no cross-file resolution order, namespace, or conflict rule. NEW, WORSE EVIDENCE the finding missed: 14 rule names are defined in MULTIPLE files, several divergently — uuid (envelope.abnf:164 hexdig-ci mixed-case vs transport.abnf:30 hexlc lowercase-only, contradicting transport's own :28 comment "accept case-insensitive"), segment (identifiers.abnf:52 vs subject-namespace.abnf:115 "segment = stack-slug"), type (envelope.abnf:149 vs subject-namespace.abnf:147 — unrelated concepts), lower (envelope.abnf:101 redefines what identifiers.abnf:33 owns, a literal rule-5 violation). So even naive concatenation yields ill-formed ABNF (RFC 5234 forbids redefinition) — per-file scoping + owner precedence is mandatory and unwritten. CORRECTIONS: (1) "not standalone-parseable" / "rule 2 unsatisfiable" is FALSE — RFC 5234 parse validity does not require rule closure; undefined references are a completeness gap, not a parse failure; all 10 files satisfy README rule 2 as stated. (2) "no composition/import mechanism specified anywhere" is OVERSTATED — README rule 5 states the owner-defines/others-reference convention, and every importing file carries an explicit per-rule import manifest naming the source file (subject-namespace.abnf:6-12, admission.abnf:13-14, capability-discovery.abnf:15-20, economics.abnf:22-32, envelope-signing.abnf:29-30, transport.abnf:5-9). A human implementer has a documented path; what is missing is the MACHINE-actionable mechanism for tools/abnf-gen. Severity stays MAJOR; reframe as "not mechanically composable: comment-level import manifests + 14 multiply-defined rule names (3+ divergent) with no specified scoping/precedence," not "not parseable."

### D21. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `specs/grammar/envelope.abnf:224 vs capability-discovery.abnf:49 vs subject-namespace.abnf:185 (`capability` rule)`  
**Finding:** `capability-tag` — a single terminal alphabet — is defined three times with structurally divergent productions inside the ratified set, violating grammar/README rule 5 ('An alphabet defined twice is an alphabet that will drift') in the very documents that state it; a generator emits three different regexes for one terminal, and envelope.abnf's version structurally admits single-character tags the other two forbid.  
**Verify:** Reproduced from source; the finding is real and MAJOR stands — and it is actually STRONGER than stated, because it is not just a README-rule-5 violation but a violated normative MUST NOT between ratified siblings.

REPRODUCED: (1) Triple definition confirmed. envelope.abnf:224 `capability-tag = cap-head *( "-" cap-run )` with `cap-head = lower *( lower / DIGIT )`; capability-discovery.abnf:49 `capability-tag = lower 1*tag-sym *( "-" 1*tag-sym ) / lower 1*( "-" 1*tag-sym )`; subject-namespace.abnf:185 `capability = lower ( 1*alnum *( "-" alnum-run ) / 1*( "-" alnum-run ) )`. All three cite the same CAPABILITY_TAG_RE (src/patterns.ts:21, `/^[a-z](?:[a-z0-9]|-(?!-)){0,62}[a-z0-9]$/`). grammar/README.md:22-23 rule 5 confirmed verbatim ("Terminal alphabets are defined once... An alphabet defined twice is an alphabet that will drift"). (2) Single-char divergence confirmed: envelope's cap-head alone matches "a" structurally; the 2..64 bound exists only as a comment side-condition (envelope.abnf:215-218, self-declared: "a bound the run structure below does not itself express"). The other two structurally require >=2 chars. (3) sovereignty-mode dual definition confirmed (sovereignty.abnf:82, capability-discovery.abnf:72, identical today).

STRONGER THAN CLAIMED — a live cross-RFC normative contradiction the finding under-cites: RFC-0008 §4.1 (rfc-0008-capability-discovery.md:358-363): "This document is the single normative owner of the capability-identifier grammar — capability-tag... RFC-0002's subject grammar and capability taxonomy reference the rules defined here and MUST NOT transcribe a second copy"; and §8 (:627-630): "Siblings (RFC-0002's taxonomy and subject grammar, RFC-0003's requirements[]) reference them and MUST NOT redefine them." Yet ratified RFC-0003's envelope.abnf:219 asserts contradictory ownership ("Co-owned with RFC-0002") and defines a second copy at :224, and RFC-0002's subject-namespace.abnf:185 transcribes a third. The reference-not-redefine mechanism exists and is used in the very same files for did/segment/signature (envelope.abnf:42-51, subject-namespace.abnf:196-197 "REFERENCED, not redefined (rule 5)"), so this is not an ABNF-can't-import inevitability.

TWO OVERSTATEMENTS in the finding's evidence (do not change severity): (a) "three structurally divergent productions" — capability-discovery.abnf:49 and subject-namespace.abnf:185 are structurally EQUIVALENT (same language; tag-sym ≡ alnum, `lower` merely factored out); only envelope's production diverges. It is two spellings plus one divergent structure, not three-way divergence. (b) The accepted LANGUAGES do not diverge today: under the series' own documented side-condition convention (envelope.abnf:215-218 carries the 2..64 bound; subject-namespace.abnf:186-188 says the 2-64 bound is vector-enforced), all three denote exactly the CAPABILITY_TAG_RE language, and each site is vector-bound (envelope/requirements-bad-tag; capability/reject-single-char; capability/reject-trailing-hyphen). The divergence bites only a structure-only generator that ignores comment side-conditions — which is precisely the generator posture rule 3 (README:17-19) mandates ("Regexes... derive from these files"), so the risk is real for the clean-room second implementation, but it is a drift/generator hazard plus a normative-process contradiction, not a deployed accept-mismatch. Hence MAJOR (dangling normative MUST NOT + generator divergence), not BLOCKER.

### D22. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `myelin-rfc0001 package.json exports (no ./wire, ./vectors, ./schemas); specs/CONFORMANCE.md 'Each consumer MUST'; cortex package.json:55; cortex src/bus/myelin/vendor/`  
**Finding:** The CONFORMANCE consumer obligations are unrunnable as shipped in PR #230: vectors and schemas are not exported from the package, so no consumer can satisfy MUST-1 ('Pin a version … and run its vectors in CI'); meanwhile cortex pins myelin by raw git hash, vendors the schema and validator with no divergence gate (MUST-3), and has zero conformance tests — every consumer is born non-conformant to a Ratified regime the moment the PR merges.  
**Verify:** not-run

### D23. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `cortex src/cli/cortex/commands/stack.ts:75, network.ts:3838, src/bus/review-consumer.ts:1454+:1474, src/common/types/capability.ts:172, src/cli/cortex/commands/offer.ts:131`  
**Finding:** Every drift site the design doc inventories is still live, and several now accept identifiers the ratified RFC-0001 kebab-strict grammar forbids — cortex will mint underscore-bearing stack slugs and uppercase segments that no conforming parser can round-trip, and nothing (no vector run, no CI gate) measures this distance.  
**Verify:** not-run

### D24. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `myelin-rfc0001 docs/design-shared-wire-codec.md §4.2, §8 (WP-4), §10.1; vs rfc-0001 front-matter (ratified 2026-07-13), specs/CONFORMANCE.md 'Changing the wire', rfc-0010`  
**Finding:** The design doc — the only plan of record for the reuse question — is stale against the series it must now implement: its keystone open decision is already resolved, its change-control premise contradicts ADR-0001's v1 posture, and RFC-0010's refusal-object grammar postdates it entirely, leaving that wire surface unassigned in the library plan.  
**Verify:** not-run

### D25. [MAJOR] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `rfc-0004-envelope-signing.md front-matter `generated:` list, second entry`  
**Finding:** RFC-0004's machine-readable front matter — which agents are instructed to ground on — lists its own normative grammar file as a *generated* artifact, a category error under the CONFORMANCE precedence chain: a manifest-driven regenerate gate would attempt to regenerate the normative source from itself.  
**Verify:** not-run

### D26. [MINOR (was MAJOR)] SEAM INTEGRITY (PLAN §6.1)
**Where:** `myelin-rfc0001: specs/rfc/rfc-0006-membership-and-admission.md:832 vs rfc-0010-rate-limit-and-refusal-taxonomy.md (no OD-5, no openDecisions at all)`  
**Finding:** Same dangling-OD-label class, second instance: RFC-0006's Security Considerations says the charset-coercion collision hazard is "carved to RFC-0010, OD-5", but the ratified RFC-0010 carries no OD-5 and no openDecisions list of any kind; the carve actually landed as RFC-0010 §3.3's normative reject-not-coerce rule ("the RFC-0006 D15 carve, landed here") plus the `admissionKeyPrincipalSegment` collision vectors.  
**Verify:** DOWNGRADE MAJOR → MINOR (wording). The finding misparses the anchor. "OD-5" at rfc-0006-membership-and-admission.md:832 is RFC-0006's OWN open-decision label, not a pointer into RFC-0010's (nonexistent) ledger: the 2026-07-12 lifecycle entry (0006:1182) defines it — "Records six open decisions (... OD-5 charset-coercion rejection ...)" — and the 2026-07-13 entry (0006:1183) tracks it ("OD-2/OD-3/OD-4/OD-5 untouched"). The carve IS OD-5's recorded disposition: grill-logs/rfc-0006.md:88 ("D15 → carve to RFC-0010. Charset-coercion: KV-key collision vectors + admissionKeyPrincipalSegment conformance op move to RFC-0010") and the 2026-07-14 weave entry (0006:1185, "carved to RFC-0010 ... (D15/D20)"). The document's citation convention confirms the parse: the sibling bullet at 0006:848 cites "(OD-1)" the same way for RFC-0006's own OD-1. RFC-0010 was drafted and ratified same-day (0010:430) with zero openDecisions — nothing in the series expects an OD-5 there, so "no OD-5 in 0010" proves no dangle. The claimed defect class ("dangling OD-label, second instance") does not reproduce; the anchor resolves entirely within 0006 (1182 → D15 → 832). What survives is only the comma-order ambiguity — "(carved to RFC-0010, OD-5)" invites exactly this misparse; "(OD-5, carved to RFC-0010)" or "(our OD-5; D15)" would be immune. That is a MINOR wording fix, and the finding itself concedes the normative substance (0010 §3.3 reject-not-coerce + admissionKeyPrincipalSegment vectors) landed consistently.

### D27. [MINOR (was MAJOR)] Security Composition (PLAN §6.3) — cross-RFC security-model composition across the 11-RFC wire-protocol series
**Where:** `rfc-0006-membership-and-admission.md §7.3/§7.4 (lines 514-587) vs rfc-0005-sovereignty-and-boundary-crossing.md §6 ingress decision (lines 478-543) + §10 (line 754)`  
**Finding:** RFC-0006 makes network_id cryptographically load-bearing in admission (to close a cross-network confused-deputy), but RFC-0005's ingress authorization is network-blind: the §6 decision procedure keys only on (principal ∈ imported_principals, subject ∈ local_scope, requirements ⊆ max_capabilities) and never reads the network dimension. The network scoping RFC-0006 binds is silently dropped one layer up, permitting cross-network trust bleed that neither RFC flags.  
**Verify:** DOWNGRADE MAJOR→MINOR. The finding's facts reproduce but its security framing fails. Reproduced: RFC-0005 §6 ingress (lines 483-544) keys only on principal/subject/capabilities with no network input; `partner_network` appears exactly once (line 754 — in §11 Privacy, not §10 as cited) and is consumed by no normative rule; RFC-0006 §7.3/§7.4 quotes are verbatim-accurate; neither §10 (RFC-0005) nor §12 (RFC-0006) mentions ingress network-blindness. Refuted, three grounds: (1) No contradiction — the two mechanisms protect different assets. RFC-0006's network_id binding closes a REGISTRY-plane threat ("request_id → wrong-network-row substitution" by a global admin, §7.3) and §7.4 states it closes that confused-deputy "end to end" at its own layer; RFC-0006 nowhere exports a network-scoped-trust obligation to partner ingress, so nothing is "silently dropped one layer up." (2) Per-principal ingress trust is an explicit ratified decision, not an oversight: grill-logs/rfc-0005.md:36-43, RFC5-D9 "Trust in the ingress mapping is per-PRINCIPAL," batch-confirmed final 2026-07-15. Signed-envelope trust is path-independent by design — the crypto rides the envelope, not the pipe. (3) The "bleed" scenario grants P nothing beyond the local_scope/max_capabilities ceiling O explicitly configured for P; "trust P only on N1" is a property no RFC promises, and it is inexpressible at EVERY layer (subject-namespace.abnf:63 federated-subject has no network segment; envelope grammar has no network field) — a design-space limitation, not a dropped control. Also, "partner_network is dead" is overstated: the deployed generateFederationScript consumes it for NSC provisioning (docs/migrations/0001-vocabulary-grilled-2026-05.md:471-473), the other layer of §7's two-layer contract, which RFC-0005 declares out of scope. Surviving MINOR residue: (a) partner_network is named in RFC-0005 §11's SovereigntyPolicy enumeration with no semantics stated anywhere in the series — add a "consumed only by NSC provisioning, out of scope" note or drop it; (b) RFC-0005 should carry a one-line Security Consideration recording that ingress trust is deliberately network-agnostic (D9), so the deliberateness is on the record in the ratified text rather than only in the grill log.

### D28. [MINOR (was MAJOR)] Security Composition (PLAN §6.3) — cross-RFC security-model composition across the 11-RFC wire-protocol series
**Where:** `rfc-0005-sovereignty-and-boundary-crossing.md §2.4 (lines 266-283) vs rfc-0004-envelope-signing.md §5.5 D12 (line ~584)`  
**Finding:** RFC-0005 enforces max_hop as a forwarding-TTL against len(signed_by chain), asserting the chain length as a trustworthy hop-count observable — but RFC-0004 D12 declares trailing stamps strippable, so an admitted adversary can tail-strip and re-append to undercount hops and evade the loop bound. RFC-0004 flags the analogous ingress case (F-5) but not the max_hop case, and RFC-0005 §2.4 acknowledges neither strippability nor D12.  
**Verify:** DOWNGRADE MAJOR→MINOR (missing caveat/cross-ref, not a security-composition defect). The mechanics reproduce: RFC-0005 §2.4 (rfc-0005-sovereignty-and-boundary-crossing.md:266-273) enforces len(signed_by chain)−1 > max_hop as a forwarding TTL, and RFC-0004 D12 (rfc-0004-envelope-signing.md:585-589) makes trailing stamps strippable, so an admitted forwarder can strip-to-k and re-append to undercount hops; RFC-0005 indeed never mentions strippability (grep: zero "strip" hits). But the composition is not a contradiction and not a security hole. (1) No contradiction: §2.4's "no mutable counter is needed" is a mechanism statement vs. the retired decrement semantic, not a tamper-proofness claim; D12 openly records the strippability residual for the H4-successor RFC — the texts compose consistently into "TTL over a cooperatively-maintained observable," which is what every TTL is (the retired decrement counter was resettable by a malicious forwarder signature-free; chain-length is strictly stronger — manipulation requires an admitted, attributable signer stamping itself onto the chain). (2) No new adversarial capability: RFC-0005 §10's threat model (lines 673-678) never lists max_hop as an anti-adversary control; finding 4 (lines 700-703) frames it as bounding a federation LOOP — an honest-forwarder misconfiguration phenomenon, against which chain-length works since honest forwarders never strip. A loop sustained by an adversary re-stripping every cycle is flooding by an admitted publisher, who can equally re-originate the payload as a fresh envelope (self as s[0], arbitrary max_hop) or replay verbatim bytes — no hop-count design bounds that; RFC-0010 rate limits do. (3) The F-5 asymmetry is coherent: F-5 (rfc-0004:987-989) is a flag-day follow-up because the ingress gate drives an AUTHORIZATION decision off strippable s[n-1] — a privilege consequence; max_hop is not an authorization input and undercounting it has no privilege consequence (origin anchor s[0], sovereignty block, classification prefix, partner ceilings all truncation-safe/immutable). Surviving item: add a one-line caveat in RFC-0005 §2.4 cross-referencing RFC-0004 D12 that the hop-count observable is cooperative (admitted forwarders can attributably undercount), so max_hop reads as loop hygiene, not an adversarial control — the audit's own MINOR (wording) class.

### D29. [MINOR (was MAJOR)] VECTOR CROSS-BINDING + GRAMMAR (PLAN §6.4)
**Where:** `myelin-rfc0001/specs/vectors/subject-namespace/vectors.json:8,10,11,12 and specs/vectors/capability-discovery/vectors.json:180-283 (vectors 14-17)`  
**Finding:** Pre-cut/HISTORICAL ACCEPT vectors are distinguishable only by prose 'why' text — the vector schema has no era/window field — so by the series' own rules a post-flag-day-R conformance run must fail them, and rule 3 already classifies them as defects.  
**Verify:** DOWNGRADE MAJOR → MINOR. The factual core is real but the contradiction claim fails on three counts, each reproducible from source.

(1) "A post-flag-day-R conformance run must fail them" is wrong for the subject-namespace quads because the operation under test is era-independent. The vectors' kind is encodeDidSegment, which identifiers.abnf §3 (specs/grammar/identifiers.abnf:180-183) defines as pure byte-rewriting: "maps a bare DID to an '@'-prefixed NATS subject segment by rewriting ':' -> '-' and '.' -> '--'; every other octet passes through verbatim." That total function produces '@did-mf-forge' from the byte-string 'did:mf:forge' in any era; a post-R implementation passes encode/did-forge AND encode/agent-dotform-subject with the same mapper. The grammar's rejection of classless DIDs binds at PARSE (inv/legacy-classless, a different kind), so there is no vector-vs-grammar disagreement for rule 3 to trigger — the vectors pin the mapping, the grammar governs the language.

(2) Even for validate-before-encode implementations, the ratified prose already disposes of the era question, so the vectors are not "defects under its own constitution." RFC-0002 §13 (rfc-0002-subject-namespace.md:1025-1029) states the exact carve-out: "before R the pre-cut '@'-encode vectors pin the retired byte-behaviour and are not a conformance target for post-R emitters" — a specific, ratified exception sitting immediately after the same section's restatement of rule 3. And rfc-0002:455 repeats it: "their pre-cut encode vectors are retained only to pin the pre-cut byte-behaviour."

(3) For capability-discovery vectors 14-17, the finding has it backwards: a post-R run is REQUIRED to pass them, not fail them. RFC-0008 §11 item 5 (rfc-0008-capability-discovery.md:823-825): "(Historical decode-compat) Recognise the retired F-11 registration per the advertisement/* vectors — including identity-mismatch, dual_field_conflict — for as long as stored registrations may exist." The classless did:mf:luna is stored-record decode-compat by ratified conformance mandate, with an explicit temporal bound, and the same section demonstrates the retirement mechanism (gap vectors superseded "per the delete-with-note rule", i.e. vectors README rule 2 — the series' machinery for retiring vectors at era boundaries).

What SURVIVES (the MINOR): the vector schema (specs/vectors/README.md:56-66 — id/rfc/kind/input/expect/why only) indeed has no machine-readable era/status field, and CONFORMANCE.md's contract ("conforms if and only if it passes every vector under the path named in that RFC's vectors front-matter field") gives a runner no hook to express RFC-0002 §13's "not a conformance target for post-R emitters" or RFC-0008 §11.5's "for as long as stored registrations may exist" — those bounds live only in prose and why-text. A strict validate-first post-R implementation would need a hand-maintained exclusion list, the exact drift CONFORMANCE.md itself condemns ("Hand-maintaining any downstream artifact reintroduces exactly the drift this replaces"). Recommend a schema addition (e.g. era: "pre-cut" | "decode-compat" | "terminal") or a separate historical.json file — a hygiene improvement, not a cross-RFC contradiction or a self-declared-defect condition.

### D30. [MINOR (was MAJOR)] Gap closure vs opening audit (PLAN §6.5) + retained-open inventory (PLAN §6.6). Closure verdict: all 6 orphaned dimensions (signing, admission, transport, versioning, discovery-core, sovereignty-semantics) have Ratified RFC homes (0004/0006/0007/BCP-0001/0008/0005); ABNF + vectors columns of the coverage matrix are green (10 grammars, 10 non-empty vector dirs); criticals C-1..C-5 closed by ratified decisions. Live retained-open census: 0004=3 (properly inventoried, acceptable), 0006=2 (one stale, one conflicted), 0009=6 (deliberate charter, two defects), all others=0.
**Where:** `myelin-rfc0001/specs/rfc/rfc-0006-membership-and-admission.md:220 (restated :783-786) vs rfc-0010-rate-limit-and-refusal-taxonomy.md:96 + front matter :8`  
**Finding:** RFC-0006 OD-1 is stale and factually false: its live [OPEN DECISION] marker says the re-homing is 'blocked on drafting RFC-0010 … chartered but not yet drafted', but RFC-0010 is Ratified (2026-07-15) and itself declares it closes OD-1 — and the closure has no formal instrument, because RFC-0010's front matter declares `updates: []` against the frozen RFC-0006.  
**Verify:** MAJOR → MINOR. (1) The headline evidence no longer reproduces: rfc-0006:219 now reads "**OD-1 is now CLOSED (2026-07-15):** RFC-0010 is Ratified and retitled `admission.md`…" — the quoted [OPEN DECISION] marker was removed by commit 445705c (2026-07-15 10:35), 13 min after RFC-0010's ratification commit 8c1aeab. (2) The "no formal instrument — only conformant vehicle is updates:[\"0006\"]" prong is REFUTED by a ratified sibling: BCP-0001 §2.1 ("Under single-principal ratification (v1), a Ratified RFC in this series is a living spec, not a stone tablet"), §2.2 ("Suspended in v1…: immutability-once-Ratified"), and §9 step 1 ("the RFC is revised in place under the living-spec model (§2.1). Reinstate-target: … a new RFC carrying Updates: NNNN"). The `updates:` header is the reinstate-target mechanism, suspended under ADR-0001; in-place revision of RFC-0006 is the conformant v1 closure vehicle and is what occurred. The "A Ratified RFC is frozen" line (rfc-0006:1178) is template boilerplate overridden by the ratified BCP-0001 v1 regime. (3) Surviving MINOR: the closure sweep missed the §11 restatement — rfc-0006:782-786 still says RFC-0010's home is "chartered but not yet drafted" and resolves "once that RFC exists", contradicting rfc-0006:219 and RFC-0010's `status: Ratified` front matter. One-line wording fix in the §11 Reserved-identifiers bullet (plus optional revision-history hygiene entry for the 445705c edit).

### D31. [MINOR (was MAJOR)] Gap closure vs opening audit (PLAN §6.5) + retained-open inventory (PLAN §6.6). Closure verdict: all 6 orphaned dimensions (signing, admission, transport, versioning, discovery-core, sovereignty-semantics) have Ratified RFC homes (0004/0006/0007/BCP-0001/0008/0005); ABNF + vectors columns of the coverage matrix are green (10 grammars, 10 non-empty vector dirs); criticals C-1..C-5 closed by ratified decisions. Live retained-open census: 0004=3 (properly inventoried, acceptable), 0006=2 (one stale, one conflicted), 0009=6 (deliberate charter, two defects), all others=0.
**Where:** `myelin-rfc0001/specs/rfc/ front matter: rfc-0003:27, rfc-0004:26 (present) vs rfc-0006, rfc-0009 (absent)`  
**Finding:** The machine-readable retained-open inventory is inconsistent: the `openDecisions` front-matter field exists only in RFC-0003 (empty) and RFC-0004 (3 entries), while RFC-0006 (retained OD-4 + stale OD-1) and RFC-0009 (6 live §5 charter markers) carry their retained decisions in prose only — so an agent enumerating the series' open set from front matter (the layer every RFC says to ground on) counts 3 when there are actually 10 live retained ODs (+1 stale).  
**Verify:** DOWNGRADE MAJOR → MINOR (hygiene/wording), with one evidence item refuted outright.

WHAT REPRODUCES: The raw asymmetry is real. `grep openDecisions specs/rfc/rfc-*.md` matches only rfc-0003-envelope.md:27 (`openDecisions: []`) and rfc-0004-envelope-signing.md:26 (3 retained entries). RFC-0006 carries one live in-place marker (`**[OPEN DECISION]** in place: the v1-PSK envelope-emit retirement date (OD-4)`, rfc-0006:125) and RFC-0009 carries six §5 headers (`[OPEN DECISION — Andreas + JC — blocked on: …]` at rfc-0009:326/333/342/349/356/376), none surfaced in any front matter. Both files open with the `Agents ground on THIS, not on prose` banner.

WHY IT IS NOT MAJOR:
1. `openDecisions` is not a series contract. The normative front-matter schema in specs/rfc/template.md (lines 1–20) defines rfc/title/status/category/obsoletes/updates/authors/signatories/created/ratified/grammar/vectors/generated/supersedes_prose — NO openDecisions key. Grep confirms neither BCP-0001 (change control) nor PLAN.md ever names the field. It was introduced ad hoc by the RFC-0004 grill (RFC-0004 §9.4: "marked in place above and in the document's `openDecisions` front matter" — a statement about THAT document) and copied by 0003. By the template, 0006/0009 are the conformant ones; 0003/0004 carry a voluntary extra. Absence of a field no schema defines is not "the machine layer", it is a convenience two documents added.
2. PLAN §6.6's actual bar is met. §6.6 reads: "Every `[OPEN DECISION — Andreas+JC]` deliberately left across the series is enumerated and consciously resolved OR accepted as a stated v1 limitation. Nothing ships silently open." The enumeration token the PLAN itself names is the in-place `[OPEN DECISION — Andreas+JC]` marker — greppable and machine-enumerable — and all 10 live ODs carry it. None is silent: RFC-0009's six are its explicit charter ("The [OPEN DECISION] labels below are retained verbatim as the successor's charter", rfc-0009:323–324) and are additionally machine-signaled by front matter `category: Informational` plus the Status memo ("REQUIRES a separate Standards-Track RFC that resolves the open decisions in §5", rfc-0009:52) — the textbook "stated v1 limitation" disposition §6.6 accepts. RFC-0006's OD-4 is marked in place per exactly that convention. The finding's counting protocol (enumerate ONLY the openDecisions front-matter key) is auditor-invented; no document promises the front matter is an exhaustive OD inventory — the banner establishes precedence for the fields it carries, not completeness over prose content.
3. One evidence item is FALSE: the claimed live/stale marker at rfc-0006:220 (OD-1). Grep finds exactly ONE `[OPEN DECISION]` marker in RFC-0006 (:125). The :220 region is a closure record, not an open marker: "**OD-1 is now CLOSED (2026-07-15):** RFC-0010 is Ratified and retitled `admission.md` to the substrate rate-limit contract… the re-homing is complete" (rfc-0006:219–221). So the "(+1 stale)" and the parent claim that 0006 is "one stale, one conflicted" misread a resolution ledger; 0006's live retained-open count is 1 (OD-4), properly marked.

RESIDUE WORTH KEEPING (the MINOR): two RFCs carry a front-matter field nine siblings lack, so an agent that generalizes from RFC-0004 §9.4's wording would undercount 3 vs 10. Cheap harmonization fix: either add `openDecisions` to 0006 (1 entry) + 0009 (6 entries) or add the field to template.md — or strike RFC-0004 §9.4's front-matter sentence. That is a wording/consistency cleanup, not a silently-open shipment: the series' own marker convention makes every live OD enumerable in one grep, and the series audit (§6.6's named mechanism) does exactly that.

### D32. [MINOR (was MAJOR)] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `the 11-RFC series as a set: specs/README.md index table (no composition RFC); myelin#10; myelin docs/architecture.md:73; cortex docs/architecture.md §4.6`  
**Finding:** L6/M6 Composition is the ONLY myelin-owned protocol layer with no RFC — the series covers L2 (0007), L3 (0002/0003/0009), L4 (0001/0004), L5 (0008) and cross-layer (0005/0006/0010, BCP) but leaves one of the five myelin layers contract-less, and nothing in specs/README.md declares this gap (its scope sentence claims the series is only M3 anyway — see the BLOCKER).  
**Verify:** DOWNGRADE MAJOR → MINOR (wording). The bare fact reproduces, but the framing "leaves one of the five myelin layers contract-less... undeclared" fails on four counts, and two of the finding's three supporting quotes are fabricated.

WHAT REPRODUCES: specs/README.md:27-39 index has no composition RFC (verified). Grep for 'composition' across the 11 RFCs finds only subject-composition wordplay, none of it L6 (finding's counts even omit rfc-0001-identifiers.md's 4 mentions, e.g. :239 "Composition of the @-segment into a full subject is specified by RFC-0002" — all identifier/subject composition, not envelope-combination patterns). cortex docs/architecture.md:281 §4.6 accurately reads "Status | Spec not yet written."

WHY IT DOWNGRADES:
1. THE SERIES WAS NEVER CHARTERED TO COVER L6. The finding's own referenced scope charter — wire-protocol-gap-analysis.md §2 ("What 'the wire protocol' is — the ten dimensions", :28-46) — enumerates ten dimensions (envelope, subjects, identity, signing, sovereignty, provenance, admission, transport, discovery/econ, versioning); composition appears ZERO times in the entire gap-analysis document (grep confirms 0 matches for L6/composition). §8's "Recommended complete RFC series" (:134-151) charters exactly the shipped set against those ten dimensions. PLAN.md §6 item 5 gates on "Gap closure vs. the opening audit" — and the opening audit contains no composition gap to close. Measuring the series against the seven-layer model instead of its own ten-dimension charter is the auditor's yardstick substitution, not a cross-RFC defect.
2. THE GAP IS DECLARED — just not in specs/README.md. myelin docs/architecture.md:71 "| **L6 Composition** | Patterns for combining envelopes (pipeline, fan-out, request/reply) | — | [#10] | spec pending |" and :166-174 "### L6 — Composition *(spec pending)* ... **Code.** None canonical ... The right take per the v4 vision is 'let it emerge' — formalize once the common patterns are visible." myelin README.md:144 and cortex §4.6 ("Spec home ... myelin#10 (open, future) ... Cortex's dependency: None at v1") say the same. The deferral is a documented, tracked, deliberate decision with a rationale — the exact remedy the finding demands ("state that L6 is out of series scope and tracked by myelin#10") already exists in the layer model's authoritative home; only the one-line restatement in specs/README.md is missing.
3. "CONTRACT-LESS" OVERSTATES: the only wire-visible piece of L6's charter (request/reply) IS normatively specified — RFC-0007 §7 pins the reply mailbox, `extensions.reply_to`, `_INBOX.{uuid}` grammar, and exact-`correlation_id` settlement (rfc-0007:412-436). What remains unspecified (pipeline, fan-out) has, per myelin architecture.md:170, no canonical code and no wire representation to contract — there is nothing for a wire RFC to pin yet.
4. FABRICATED QUOTES: the finding cites "myelin docs/architecture.md:73: 'L6 Composition … spec [#10] still open | partially implemented'" — line 71 (not 73) reads "spec pending"; "partially implemented" appears nowhere in the file. It cites "myelin README.md:170: 'L6 Composition — orchestrator + bidding in-tree; pattern spec still open (#10)'" — the only Composition line is :144, reading "pipeline / fan-out / request-reply patterns ([#10], spec pending)"; "orchestrator + bidding in-tree" appears nowhere. Both quotes fail the audit's own "quote both sides from source" bar.

RESIDUAL MINOR: specs/README.md's scope statement should carry one sentence — "L6/M6 composition patterns are out of series scope; tracked at myelin#10" — but this is subsumed by the separately-filed BLOCKER on the same sentence (README.md:5 "myelin is **M3** ... envelope and namespace" misdescribes a series that spans M2-M5 + cross-layer); any fix to that sentence necessarily states the coverage envelope, and stating it makes the L6 exclusion explicit. Not a MAJOR-class dangling ref, unbound vector, or stale status; no RFC-0011 charter is warranted against the series' actual charter.

### D33. [MINOR (was MAJOR)] LAYER LANGUAGE CLARITY — authoritative L1..L7 mapping + every place the docs conflate myelin-the-protocol / myelin-the-repo / the layer model / a stack
**Where:** `myelin-rfc0001/docs/architecture.md:23,50,72 (the canonical seven-layer reference the RFCs and README point at)`  
**Finding:** The canonical layer doc's L7 row is stale and self-contradictory about what implements L7 today: it lists 'Grove' (retired — replaced by cortex) and 'CORTEX (capability AI)' (the superseded v4 layer name, which the SAME doc says this model supersedes) — so a reader cannot tell whether 'CORTEX' at L7 means the old v4 AI-layer or the cortex repo, and the repo that actually IS the main L7 surface today is never correctly named.  
**Verify:** MAJOR → MINOR. The stale-roster half reproduces: architecture.md:23/:50/:72/:206/:208 list Grove/grove as a current L7 surface and never name lowercase cortex-the-repo (grep confirms zero occurrences), while cortex CLAUDE.md:18-20 and the authoritative compass CONTEXT-MAP.md:12 ("cortex | the-metafactory/cortex | The L7 collaboration surface") make cortex the actual L7 surface; the doc was edited 2026-07-03 and carries a maintenance obligation (:7), so it is live staleness. But the self-contradiction half is REFUTED: :17 explicitly says "CORTEX is repositioned as a Layer 7 application rather than a peer layer" — CORTEX in the L7 row is the doc executing its own repositioning, not a leftover of the superseded naming (what is superseded is the five-layer NAMING, not the CORTEX term). The claimed reader ambiguity is resolved by the doc itself at :206 ("the future CORTEX capability AI") plus the ALL-CAPS-v4-names convention — a reader CAN tell it means the repositioned v4 concept, not the repo. Blast radius is nil for the RFC series: rfc-0003/0007/0009 cite architecture.md only for §5.2/§L3 mutable-fields content, never the L7 roster, and rfc-0007:578 already names "cortex origin/main" correctly as the consumer implementation. What survives is a wording fix in an informative sibling doc (update L7 roster to "cortex · pilot · signal-collector · future apps" at :23/:50/:72/:206/:208, and drop the now-realized "future" at :206) — MINOR per the audit's own rubric, since no cross-RFC contradiction, dangling ref, or unbound vector results.

### D34. [MINOR (was MAJOR)] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `specs/CONFORMANCE.md 'myelin MUST' item 2 vs rfc-0001-identifiers.md front-matter `generated:` comment ('regenerated at the flag-day cutover (§9)') and schemas/envelope.schema.json`  
**Finding:** CONFORMANCE.md's unconditional regenerate-and-diff gate contradicts RFC-0001's flag-day deferral: the committed schema still carries the flat pre-ratification DID pattern, which the ratified dot-form grammar rejects, so the MUST-2 gate — if ever implemented as written — fails on every RFC-0001/0002 artifact, and no document states which rule wins during the pre-flag-day window.  
**Verify:** MAJOR → MINOR. Every mechanical fact reproduces: CONFORMANCE.md:88-90 MUST-2 is textually unconditional; schemas/envelope.schema.json (lines 114/178/188/220/231/232) and src/identity/types.ts:1 still carry the flat pre-cut pattern `^did:mf:[a-z](?:[a-z0-9._]|-(?!-))+$`; identifiers.abnf:153-156 mandates the class tag and vector inv/legacy-classless (invalid.json:83) rejects the flat form — so a regeneration run today would differ from the committed output; and no explicit MUST-2 carve-out sentence exists in CONFORMANCE.md, BCP-0001 §8/§9, or specs/README.md. But the finding's load-bearing clause — "no document states which rule wins during the pre-flag-day window" — fails quotation. The ratified RFC-0001 states the window rule in the exact field MUST-2 operates on: front-matter line 19 ("generated: # DERIVED from grammar; regenerated at the flag-day cutover (§9)", under "Agents ground on THIS, not on prose"), restated at §1 line 78, §9.1 item 4 (regeneration is a numbered component of release R), and §11 line 444 ("Ratification does not by itself flip live behaviour… only at the coordinated flag-day release R"). CONFORMANCE.md itself resolves interim authority at lines 69-71: "Where a generated artifact disagrees with the ABNF, the ABNF governs and the artifact is a defect" — the pre-R schema is a documented, scheduled defect, not an unreconciled ambiguity, and no consumer can ground on it as authoritative. The contradiction is also latent by the finding's own concession: no tools/abnf-gen, no generator, no MUST-2 CI job exists anywhere in the worktree (0 grep hits), so nothing can fail today, and an implementer of MUST-2 is directed by its own text into the `generated` field where the deferral annotation lives. What survives is a one-sentence completeness gap — CONFORMANCE.md MUST-2 lacks an effective-at-R qualifier for artifacts whose owning RFC defers regeneration (and RFC-0002's `generated:` field, which co-lists envelope.schema.json, carries no deferral comment at all, relying on README:46's "atomic with RFC-0001's flag-day"). That is wording-class (MINOR) on the audit's own severity scale, not a cross-RFC contradiction with no reconciliation rule.

### D35. [MINOR (was MAJOR)] IMPLEMENTATION DRIFT + THE LIBRARY PLAN (PLAN §6.2 clean-room + reuse). Net assessment: (a) The vector corpus is genuinely clean-room-grade where it counts — 271 vectors across 10 RFCs, every one schema-complete (id/rfc/kind/input/expect/why verified programmatically), rejection coverage on 8/10 RFCs, and the envelope + envelope-signing generators (specs/vectors/*/generate.ts) are deliberately self-contained (node stdlib only, no myelin import), so a Go/Rust second implementation can verify the crypto core from spec+vectors alone. What is NOT spec-complete: the grammars cannot be mechanically composed (7/10 .abnf files reference rules defined only in sibling files with no import mechanism), normative length/semantic bounds live in ABNF comments, and one field (uuid/correlation_id) has two contradictory ratified grammars. (b) The shared library must contain: the generated regex atoms + class table, the ABNF composition/side-condition resolver (currently unspecified — the hardest missing piece), the identity/subject codec (WP-2 generalized), the RFC-0004 canonicalizer, the RFC-0007/0010 token+refusal-object enums, and a language-agnostic vector-runner; home myelin M3 via ./wire, ./vectors, ./schemas exports — none of which exist in PR #230's package.json. (c) The gap is total on machinery: the ratified RFCs now carry normative `generated:` manifests and CONFORMANCE.md MUST-gates that reference a generator (tools/abnf-gen) and CI jobs that do not exist anywhere; every cortex drift site the design doc inventories is still live; and the design doc itself is stale against ratification (DID keystone resolved, dual-accept posture changed by ADR-0001, RFC-0010 uncovered). The series is ratified against an implementation that does not yet consume it, with no gate measuring the distance.
**Where:** `specs/grammar/identifiers.abnf:38-42, envelope.abnf:216-218 & :231, plus 8 'side-condition' comment sites across envelope.abnf`  
**Finding:** Normative constraints — the 1-63 octet segment bound, the 2..64 capability-tag bound, the ≥88 signature length, and several semantic conditions — live only in ABNF comments, so 'regexes are generated from the ABNF' is false for exactly the constraints that made the deployed regexes what they are; the generator has no machine-readable side-condition channel to read.  
**Verify:** MAJOR → MINOR. The claim "bounds live only in ABNF comments / no machine-readable channel" fails on 3 of 4 cited constraints. (1) The ≥88 signature bound is REFUTED outright: ratified envelope-signing.abnf:77-78 encodes exactly-88 STRUCTURALLY (`signature = 85base64-char final-quantum-2bit "=="`); envelope.abnf:231's ≥88 is explicitly the deployed loose accept-grammar that "TIGHTENS onto RFC-0004's signature at flag-day R" — the constraint lives in a ratified sibling grammar rule, not a comment. (2) The 1-63 segment bound is normative RFC-0001 body text (rfc-0001-identifiers.md:170, :275) AND machine-pinned by vector inv/segment-too-long (identifiers/invalid.json:51-55, reason "segment-length-exceeds-63"); vectors/README.md declares "Vectors bind... the normative conformance artifact... plain JSON, any language" — that IS a machine-readable channel. (3) A bound-less generated regex is unshippable by design: design-shared-wire-codec.md §3.3 CI gate 3 requires the generated regex to reject every invalid vector, so an ABNF-only laxer regex fails CI; and §3.1 already documents the honest boundary ("you cannot generate parseDid's disambiguation from a CFG") — semantic side-conditions are authored-codec-layer by stated design, each with dedicated vectors (bind/agent-prefix-*, envelope/timestamp-out-of-range-accepted). What survives as MINOR: (a) design doc §3.2 lists only *.abnf as generator input and defines no annotation/front-matter mechanism for how length bounds enter emitted regexes — vectors detect a wrong output but don't feed the generator (a real TODO in an unbuilt tool's design); (b) the capability-tag 2..64 bound is the one cited constraint with no vector pin (envelope/invalid.json has only the structural a--b vector, no length vector), so gate 3 has no tripwire for it. Design-doc incompleteness, not a cross-RFC contradiction.

---

## 6. Minor findings

- **myelin-rfc0001: specs/rfc/rfc-0007-transport-and-reliability.md:157 vs specs/grammar/sovereignty.abnf:88-94 (RFC-0005 §8 / Appendix A)** — RFC-0007 §3.1's rationale sentence "Reason tokens are **payload-plane** values, and the entire ratified payload plane is snake_case" is falsified by a sibling rule both documents otherwise acknowledge: RFC-0005 ratifies the six kebab-case `NakReasonCode` reason-tokens (`classification-mismatch`, `residency-violation`, `unknown-principal`, `scope-exceeded`, `chain-invalid`, `partner-unknown`) carried in the `SovereigntyNakDetail` PAYLOAD, and RFC-0010 §2.2 inherits the same rationale ("per the ratified payload-plane convention").
- **myelin-rfc0001: specs/admission.md:10-11 vs :29-34; specs/rfc/rfc-0010-rate-limit-and-refusal-taxonomy.md:193-194** — Retitle residue in the demoted prose file: the new banner declares the file "**informative**: the normative rules live in RFC-0010 §2–§4" and stamps "Status: Informative (superseded by RFC-0010)", but the unchanged preamble five lines later still asserts "The KV bucket name, key grammar, and entry format are the hard interoperability surface ... so those sections are fully normative" — an internal Informative-vs-normative contradiction within the same file. Separately, RFC-0010 §3 says "the prose contract formerly at `specs/admission.md`", implying a path move, while the file still lives at exactly `specs/admission.md` (the retitle changed the H1 title, not the filename).
- **rfc-0005-sovereignty-and-boundary-crossing.md §8 D7 (lines 606-641) composed with §2/§12 (lines 771-779) and RFC-0004 signing** — The D7-conformant signed sovereignty nak specifies subject, source, signed_by, and classification/pairing, but is silent on whether the nak envelope carries its own required 5-member sovereignty block. Since §2/§12 make every conformant envelope carry that block and the §8 recursion exemption is from validateEgress ONLY (not from schema validation), a schema-validating subscriber — the exact actor D7 point 2 fixes the source field for — would still reject a signed nak that omits the block, reintroducing the defect class D7 closed.
- **myelin-rfc0001/specs/vectors/capability-discovery/vectors.json:180 vs specs/vectors/admission/valid.json:156; capability-discovery 'maxConcurrent-not-positive-integer'; rate-limit/invalid.json 'retry_after_ms-required'** — Reason-token spelling drift across the fixture set for the same signer≠claimed-identity seam, plus camelCase and mixed-separator tokens.
- **myelin-rfc0001/specs/vectors/sovereignty/crossing.json (whole file) vs specs/grammar/sovereignty.abnf:89-94** — Two of the six registered NakReasonCode values — 'chain-invalid' and 'partner-unknown' — are exercised by no vector anywhere in the fixture set.
- **myelin-rfc0001/specs/rfc/rfc-0004-envelope-signing.md:1179 (Appendix A) vs specs/grammar/envelope-signing.abnf; also RFCs 0001/0002/0003** — The audit premise 'RFC-0004 Appendix A is KNOWN-STALE 83 vs 222 lines' is NOT confirmed on the current branch — it has been fixed; all four checked appendices with closed fences are byte-identical to their grammar sources.
- **myelin-rfc0001/specs/rfc/rfc-bcp-0001-wire-change-control-and-versioning.md:26 (crossRefs comment) vs rfc-0007-transport-and-reliability.md:320** — BCP-0001's front-matter crossRefs comment cites 'RFC-0007 OD-4' as a still-open slice living in 0007, but 0007's grill (woven 2026-07-15, after BCP froze 2026-07-14) resolved it normatively as D19 — RFC-0007 now has zero live OPEN DECISION markers, so the pointer dangles.
- **myelin-rfc0001/specs/rfc/rfc-0009-economics.md:326, 333, 342, 349, 376** — Five of RFC-0009's deliberately-retained successor-charter markers carry 'blocked on: unfiled' — i.e. the blocking issue was never filed — leaving the Standards-Track successor's charter anchored nowhere outside the document, contrary to the series' close-out discipline that deltas become issues.
- **myelin-rfc0001/CONTEXT.md:3 vs :11-13** — myelin's glossary is internally inconsistent in its first sentence: line 3 describes myelin as 'the M2–M6 protocol stack' (M-numbering + the word 'stack') while the glossary's own layer-model entry eight lines later defines L-numbering and bans stack-phrasings.
- **myelin-rfc0001/specs/rfc/rfc-0005-sovereignty-and-boundary-crossing.md:85,128,352,548-564** — RFC-0005 overloads the reserved term 'layer' with two ad-hoc decompositions in one document — the '§7 Two-Layer Crossing Contract' (NSC layer vs engine layer) and the 'three-layer model' (L3/L4/L2) — while myelin CONTEXT.md reserves 'layer' for the seven charters; a reader hits 'two-layer', 'three-layer', and 'seven-layer' meanings of the same word inside one RFC.
- **rfc-0006, rfc-0008, rfc-0009, rfc-bcp-0001 front-matter `generated:` fields vs rfc-0007/rfc-0010** — The `generated:` field is inconsistently typed across the series — four RFCs write `generated:\n  - []`, which YAML-parses as a list containing an empty list (`[[]]`), not an empty list — so any manifest-iterating CI or tooling hits a non-string element on 4 of 11 documents.
- **specs/vectors/README.md 'Layout' vs actual directory contents (capability-discovery/vectors.json, subject-namespace/vectors.json, sovereignty/crossing.json, envelope-signing/{canonicalize,reject,sign-verify}.json, admission/valid.json, economics/valid.json)** — Six of ten vector directories deviate from the README's declared valid/invalid/render layout — including 16 rejection vectors living inside a file named `valid.json` (admission) and an Informational-but-grammared RFC with zero rejection vectors (economics) — so CONFORMANCE.md's canonical import pattern only works verbatim for 4 RFCs and a mechanical vector-runner cannot assume the layout.

---

## 7. What this gates

The series is internally sound and clean-room-grade *on the crypto core* (self-contained generators, 271 schema-complete vectors). It is NOT yet a coordinated whole that can move past Draft-toward-adoption, on two axes:

1. **Seam sweep (mechanical).** Reconcile the capability-tag ownership across 0002/0003/0007/0008 (one owner, cited everywhere else); fix the RFC-0007 import naming the wrong owner's file; the other confirmed MAJORs. These are edits, not decisions — the living-spec model permits them in place.
2. **The two alignment questions (need grilling).** The layer-language mapping (§1) and the library/sequence plan (§2) are genuine forks, not lookups — they are pulled into their own grill dockets next.

**Immediate next steps:** (a) mechanical seam sweep from §5; (b) grill the layer-language cluster (§1) and the library-sequence cluster (§2); (c) file the library machinery gap (generator + CI + `./wire` export) as the epic that actually closes the reimplementation problem — this is the work that makes federation rest on a specified contract instead of hand-written guesses.
